# SANKHYA — provenance

This document exists so that the claim "built from mathematical foundations, not wrapped
around an existing solver" can be **checked** rather than believed. It is maintained
continuously, not written at the end.

Last updated: **Phase 6** (sparse LU), with the HiGHS comparison run. Every number and every command output below was
produced by running the command shown, on the machine described, at the commit recorded.

---

## 1. The red line

No source code from an optimization **solver** is copied, vendored, linked, or read. That
list explicitly includes CBC, Clp, HiGHS, SCIP, SoPlex, GLPK, lp_solve, OSQP, PDLP,
cuPDLP / cuPDLP-C / cuPDLPx, OR-Tools GLOP and CP-SAT, Gurobi, CPLEX and Xpress — not their
simplex, not their cuts, not their MPS readers.

HiGHS appears in this project **only** as the comparison baseline in
`bench/runners/compare.py`. It runs either as an externally installed command-line binary or,
where none is present, through the `highspy` pip package in a separate Python process. It is
never linked into SANKHYA, never a build dependency, and no part of its source informs ours.
The comparison results are in `docs/BENCHMARKS.md` section 3.

---

## 2. Dependency table

Every dependency is a general-purpose library. None of them solves an optimization problem.

| Dependency | Version | Licence | Linked? | Why it is not a solver |
|---|---|---|---|---|
| **fmt** | 10.2.1 | MIT | yes, static | String formatting. Produces text from values. |
| **CLI11** | 2.4.2 | BSD-3-Clause | header-only | Command-line argument parsing. |
| **nlohmann/json** | 3.11.3 | MIT | header-only | JSON serialisation for the `--stats` result blob. |
| **OpenMP runtime** (libgomp with GCC, libomp with Clang) | as shipped by the compiler | GPL-3.0 with the GCC Runtime Library Exception / Apache-2.0 with LLVM exception | optional, found by CMake | The compiler's own thread runtime for `#pragma omp parallel for`. Schedules loops; contains no numerical code of any kind. |
| **zlib** | 1.3.1 | zlib | yes, static | DEFLATE decompression, for `.mps.gz` inputs (Phase 2). |
| **GoogleTest** | 1.14.0 | BSD-3-Clause | test binary only | Unit test framework. Never linked into `sankhya_core`. |
| **CUDA runtime (cudart)** | as shipped with CUDA Toolkit | NVIDIA SDK Licence | yes, when `SANKHYA_ENABLE_CUDA=ON` | Device enumeration and the probe in `src/gpu/device.cu`. No kernel launch, no transfer. |
| **cuSPARSE** | as shipped with CUDA Toolkit | NVIDIA SDK Licence | yes, when `SANKHYA_ENABLE_CUDA=ON` | Sparse matrix-vector products on GPU (`cusparseSpMV`). Supplies the A x and A^T y arithmetic primitives called by the GPU PDHG iteration in `src/gpu/pdhg_gpu.cu` (#17), in the same way that a BLAS supplies `dgemv` to a simplex without being a simplex. Not an optimisation algorithm. |
| **cuDSS** | 0.8.0 tested (`nvidia-cudss-cu12` 0.8.0.10 wheel from PyPI) | NVIDIA Math Libraries SDK Licence (v. 10 Feb 2022) | optional, dynamic, only when `SANKHYA_ENABLE_CUDSS=ON` (default OFF, needs `SANKHYA_ENABLE_CUDA`); found on the system through `CUDSS_ROOT`, never vendored or redistributed | Sparse direct solution of A x = b on the GPU: reordering, symbolic and numeric LDL^T, triangular solves (`src/gpu/cudss_factor.cpp`). The interior point hands it the normal equations it has assembled, as it hands them to the CPU LDL^T; it formulates and solves no optimization problem. Licence analysis: judgement call 28. |
| **cuBLAS** | as shipped with CUDA Toolkit | NVIDIA SDK Licence | **not linked** | Dense linear algebra on GPU. Not needed by the current GPU backend; listed here so the absence is a decision, not an oversight. |
| **CUB (CCCL)** | as shipped with CUDA Toolkit | BSD-3-Clause | header-only, when `SANKHYA_ENABLE_CUDA=ON` | Device-wide parallel reductions (`DeviceReduce::Sum`) used by the GPU PDHG movement and interaction scalars in `src/gpu/pdhg_gpu.cu` (#17). Ships inside the CUDA Toolkit as part of NVIDIA CCCL; no separate fetch or link step. Not an optimisation algorithm. |
| **highspy** | pip, benchmark only | MIT | **never linked** | The HiGHS solver, used ONLY as the comparison baseline in `bench/runners/compare.py`. It runs in a separate Python process, is not a build dependency, and nothing in `src/` knows it exists. Its source does not inform ours - see the red line in section 1. |
| **PuLP** | 3.3.2 and 4.0.0, pip, optional | MIT | **never linked** | A modelling library, not a solver (the CBC binary bundled with 3.x is never invoked here; 4.x stores the model in its own Rust extension and bundles no solver - see judgement call 20). #536's `bindings/python/sankhya/adapters/pulp_solver.py` implements PuLP's own `LpSolver` interface and walks `LpProblem` to build a `sankhya.Model`; SANKHYA is the only thing that solves anything. Optional: importing `sankhya` never imports `pulp`. |
| **Pyomo** | 6.10.1, pip, optional | BSD-3-Clause | **never linked** | A modelling library. #536's `bindings/python/sankhya/adapters/pyomo_plugin.py` registers against `pyomo.contrib.solver`'s `SolverBase` interface and reads coefficients via Pyomo's own public `generate_standard_repn` utility (`pyomo/repn/standard_repn.py`, the same one Pyomo's built-in LP/NL writers use) - not by writing or reading any file Pyomo or SANKHYA produces. Optional, same as PuLP. |
| **CVXPY** | 1.9.3, pip, optional | Apache-2.0 | **never linked** | A modelling library whose OWN solver plugins hook into its internal cone-canonicalization reduction chain - #536's `bindings/python/sankhya/adapters/cvxpy_solver.py` deliberately does not, to stay stable across CVXPY releases, and instead walks `Problem.variables()` / `.constraints` / `.objective` (CVXPY's public surface) directly, registered through CVXPY's own documented `solve(method=...)` escape hatch. Optional, same as PuLP. |

Considered, and not present. Each was allowed by the policy above; none turned out to be
needed, and none is linked, vendored or fetched:

| Dependency | Licence | Status |
|---|---|---|
| pybind11 | BSD-3-Clause | Not used. The Python bindings (#59, #129) are `ctypes` over the C API and compile nothing. |
| AMD / COLAMD ordering | per-module | Not used as a library. The sparse LDL^T (#70) carries its own ordering, written from the AMD paper (Amestoy, Davis & Duff 1996) for #193 - approximate minimum degree on a quotient graph - with no code from SuiteSparse or anywhere else; the LU orders by Markowitz counts. |
| Eigen | MPL-2.0 | Not used. `DenseLu` (`src/simplex/dense_lu.cpp`) is the reference oracle the sparse LU is tested against. |
| NCCL | BSD-3-Clause | Not used (#295). NVIDIA's collective-communication library, not a solver, so allowed; for two cards on one node an all-gather of the A^T y partials over `cudaMemcpyPeerAsync` moves the bytes a ring all-reduce moves over the same NVLink, and NCCL picks its reduction order by its own algorithm selection, which would tie the multi-GPU answer to NCCL's tuning instead of a fixed order. Judgement call 29. |


Benchmark data that carries a licence of its own. Instance data is not code and not a
solver; it is listed here because its licence asks for attribution.

| Dataset | Version | Licence | Committed? | What it is and how it is used |
|---|---|---|---|---|
| **QPLIB** (#492) | as fetched: `data/qplib/reference.json` records the URL and sha256 of the listing, `qplib.solu`, every instance page, `.qplib` file and solution point read | CC-BY 4.0. Every page of https://qplib.zib.de states "QPLIB is licensed under CC-BY 4.0." (linking https://creativecommons.org/licenses/by/4.0/), and the site's history dates it: "August, 30, 2021: QPLIB is now licensed under CC-BY 4.0." The attribution the licence asks for is the article the site asks users to cite: Furini et al., *QPLIB: a library of quadratic programming instances*, Mathematical Programming Computation 11 (2019), DOI 10.1007/s12532-018-0147-4 | the manifest; and, as test fixtures in `bench/runners/testdata/qplib/`, one instance file and its solution point (QPLIB_0018) byte for byte plus short excerpts of four site pages, each headed by its URL, fetch date and the page's sha256 | Quadratic programming instances and their best known objective values. `bench/runners/fetch_qplib.py` downloads them at run time; `bench/runners/qplib_format.py` converts `.qplib` to QPS; nothing is linked. The site's footer also says "Website (c) 2017-2025 by Zuse Institute Berlin and GAMS. All rights reserved.": see judgement call 30 on the page excerpts. |

---

## 2b. Toolchains this has been built and tested with

PS26119 asks for a foundation someone else can pick up, and "it builds here" is not that.
These are the toolchains the evidence in `docs/BENCHMARKS.md` was actually produced with, and
the ones CI exercises on every push. `scripts/preflight.sh` reports what it finds on the
machine it runs on, and `scripts/configure.sh` chooses among them rather than trusting the
order of `PATH`.

| Where | Compiler | Build system | Python | What it is the authority on |
|---|---|---|---|---|
| CI, `ubuntu-latest` | the runner's default GCC | CMake >= 3.20, Ninja | 3.x as shipped | `-Wall -Wextra -Werror` cleanliness, the ASan/UBSan run, and the Netlib gates. A portability claim rests on the Linux build. |
| Development, Windows 11 | GCC 16.1.0, MSYS2 UCRT64 (`C:\msys64\ucrt64\bin\g++.exe`) | CMake 4.3.3, Ninja 1.13.2 | 3.11.9 | Every benchmark CSV in `bench/results/` whose `machine` column reads `Windows-AMD64`. |
| Development, Windows 11 (fallback) | GCC 13.2.0, Strawberry Perl MinGW-W64 | as shipped | 3.x | A second Windows compiler the configure script accepts; no committed CSV comes from it. |

**One toolchain must never be selected**: a MinGW 6.3.0 that predates C++20 and sits on
`PATH` on at least one development box. `scripts/configure.sh` rejects any `g++` reporting a
major version below 10, which is why it exists rather than the build simply calling `cmake`.

Third-party sources are pinned by tag in `CMakeLists.txt`, and `docs/sbom.spdx.json` - the
SPDX 2.3 bill of materials - is generated from those pins by `tools/make_sbom.py`. CI runs
`tools/make_sbom.py --check`, so a dependency added to the build without an entry in the
table above turns the tree red instead of passing quietly.

## 3. Algorithm citation table

Every algorithm we implement is cited at its implementation site as well as here. The
citation records where the *mathematics* came from; the code is written from the
mathematics, not transcribed from anyone's implementation.

| Algorithm | Citation | File |
|---|---|---|
| Compressed-column storage, counting-sort transpose | Davis, *Direct Methods for Sparse Linear Systems* (SIAM, 2006), ch. 2 | `src/la/sparse.cpp` |
| Dense-accumulator sparse vector (FTRAN/BTRAN result pattern) | Davis, ibid.; Hall & McKinnon on hyper-sparsity | `include/sankhya/sparse.hpp`, `src/la/sparse.cpp` |
| Markowitz threshold pivoting (constant only, so far) | Suhl & Suhl, *Computing sparse LU factorizations for large-scale linear programming bases* | `include/sankhya/tolerances.hpp` |
| MPS format: sections, RANGES and BOUNDS semantics | IBM MPS specification; Maros, *Computational Techniques of the Simplex Method* (Kluwer, 2003), appendix A | `src/io/mps_reader.cpp` |
| CPLEX LP format | Public CPLEX and Gurobi reference manuals (documentation only) | `src/io/lp_reader.cpp` |
| Dense LU with partial pivoting; transposed triangular solves | Golub & Van Loan, *Matrix Computations* (4th ed.), sections 3.2 and 3.4 | `src/simplex/dense_lu.cpp` |
| Sparse LU with Markowitz pivoting and threshold stability | Markowitz, *The elimination form of the inverse and its application to linear programming*, Management Science 3 (1957); Suhl & Suhl, *Computing sparse LU factorizations for large-scale linear programming bases*, ORSA J. Computing 2 (1990); Duff, Erisman & Reid, *Direct Methods for Sparse Matrices* (2nd ed., 2017), ch. 7-8 | `src/la/lu.cpp`, `src/la/lu_eliminate.cpp` |
| LU singleton fast path (#463): a column-singleton pivot takes its U row from a row-wise copy of the values instead of opening every column its row crosses, and the threshold test reads a cached column maximum; the same pivots, factors and solves as the general update, bit for bit (tested against the reference elimination and `DenseLu`) | Suhl & Suhl, *Computing sparse LU factorizations for large-scale linear programming bases*, ORSA J. Computing 2 (1990); Koberstein, *The dual simplex method, techniques for a fast and stable implementation*, PhD thesis, Paderborn (2005), sec. 5.3 | `src/la/lu_eliminate.cpp`, `src/la/lu_workspace.hpp` |
| Basis update, product form of the inverse | Dantzig & Orchard-Hays, *The product form for the inverse in the simplex method*, Mathematical Tables and Other Aids to Computation 8 (1954) | `src/la/lu.cpp` |
| Basis update, Forrest-Tomlin (#279) - a row eta computed by a partial BTRAN through U, applied between L and U, in place of an eta appended outside both | Forrest & Tomlin, *Updated triangular factors of the basis to maintain sparsity in the product form simplex method*, Mathematical Programming 2 (1972); the implementable form followed here is Huangfu & Hall, *Novel update techniques for the revised simplex method*, ERGO 13-001 (2012), sec. 2.1 | `src/la/lu.cpp` (`update_forrest_tomlin`) |
| Bounded-variable revised primal simplex | Dantzig, *Linear Programming and Extensions* (1963); Chvátal, *Linear Programming* (1983), ch. 3 and 8 | `src/simplex/primal_simplex.cpp` |
| Piecewise-linear (composite) phase 1, no artificial variables | Maros, *Computational Techniques of the Simplex Method*, ch. 9 | `src/simplex/primal_simplex.cpp` |
| Bounded-variable revised dual simplex with the bound-flipping ratio test, dual devex pricing, artificial bounds for dual infeasibility, warm start from a basis (#65; `--option algorithm=dual-simplex`, and the branch-and-bound node engine) | Lemke, *The dual method of solving the linear programming problem*, Naval Research Logistics Quarterly 1 (1954); Maros, *Computational Techniques of the Simplex Method*, ch. 10; Koberstein, *The dual simplex method, techniques for a fast and stable implementation*, PhD thesis, Paderborn (2005), sec. 3.3 and 4.5; Forrest & Goldfarb (1992), above, sec. 3 for the dual weights | `src/simplex/dual_simplex.cpp`, shared state in `src/simplex/simplex_core.hpp` |
| Bland's anti-cycling rule | Chvátal, *Linear Programming*, ch. 3 | `src/simplex/primal_simplex.cpp` |
| Devex pricing (default since #66 was re-measured; `--option pricing=dantzig` selects the old rule) | Forrest & Goldfarb, *Steepest-edge simplex algorithms for linear programming*, Mathematical Programming 57 (1992); the approximation is Harris (1973), below | `src/simplex/primal_simplex.cpp` |
| Harris two-pass ratio test, long-step bound flipping (opt-in; `--option ratio_test=harris`, #67) | Harris, P.M.J., *Pivot selection methods of the Devex LP code*, Mathematical Programming 5 (1973), 1-28; long-step generalises the piecewise-linear phase 1 already cited to Maros below | `src/simplex/primal_simplex.cpp` |
| Exact rational tableau simplex (test oracle) | Chvátal, *Linear Programming*, ch. 2–3 | `tests/oracles/rational_simplex.cpp` |
| Primal-dual hybrid gradient (the base iteration) | Chambolle & Pock, *A first-order primal-dual algorithm for convex problems with applications to imaging*, JMIV 40(1), 2011, Algorithm 1 | `src/pdhg/pdhg.cpp` |
| Adaptive step size, primal weight, restarts | Applegate et al., *Practical Large-Scale Linear Programming using Primal-Dual Hybrid Gradient* (PDLP), NeurIPS 2021, sections 3.1, 3.2, 4.3 | `src/pdhg/pdhg.cpp` |
| Geometric off-tick evaluation schedule (opt-in; `--option pdhg_geometric_evaluation`, #480) | GitHub Issue #480 | `src/pdhg/pdhg.cpp` |
| GPU-oriented restarted PDHG (design reference) | Lu & Yang, *cuPDLP.jl*, arXiv:2311.12180 | `src/pdhg/pdhg.cpp` |
| GPU PDHG on-device iteration loop via CUDA Graphs (#478, option `gpu_on_device_loop`, off by default): one iteration captured as a CUDA graph and replayed `kPdhgDeviceLoopBlock` times per host synchronisation; the adaptive step rule and the commit of an accepted step on the device; convergence evaluation stays on the host on a stride of accepted steps | Lu & Yang, *cuPDLP.jl*, arXiv:2311.12180; NVIDIA, *CUDA C++ Programming Guide*, chapter "CUDA Graphs" | `src/gpu/pdhg_graph.cu` |
| Fixed-order device reductions and a deterministic GPU PDHG (#478, #383): each block's sum written to its own slot and the slots summed by one block in an order fixed by the thread index (no atomics, every GPU run); under `deterministic=true` both sparse products as non-transpose `CUSPARSE_SPMV_CSR_ALG2` products on A and on an explicit A^T in CSR | Higham, *Accuracy and Stability of Numerical Algorithms*, 2nd ed., SIAM 2002, section 4.1 (summation order); NVIDIA cuSPARSE documentation, `cusparseSpMV` (the algorithms and which of them are bit-wise repeatable); NVIDIA CUB documentation, `cub::BlockReduce` | `src/gpu/pdhg_reduce.cuh`, `src/gpu/pdhg_gpu.cu`, `src/gpu/pdhg_graph.cu` |
| Restarted PDHG on the device (#17): the same iteration with cuSPARSE SpMV, elementwise CUDA kernels for the coordinate updates and CUB reductions for the movement and interaction terms; preconditioning, residuals and restarts on the host; every device failure falls back to the CPU engine | Chambolle & Pock (2011) and Applegate et al. (PDLP, 2021) as above; Lu & Yang, arXiv:2311.12180, for the host/device split only | `src/gpu/pdhg_gpu.cu` |
| Batched PDHG node bounds (#520; `gpu_batch_nodes` and `gpu_batch_strong_branching`, off by default): K LPs that share A and differ in their column boxes (open branch-and-bound nodes, or the 2K children of K strong-branching candidates) iterated together as n x K and m x K blocks - Chambolle-Pock with extrapolation, Ruiz plus Pock-Chambolle scaling, a constant step 0.9 / ||A||_2, per-LP primal weights and per-LP restarts to the average on the weighted KKT error - for a fixed iteration budget; each LP's bound is then the Neumaier-Shcherbina safe bound of its duals over ITS OWN box, valid for any duals, used only to prune and order (never to accept a point). A CPU reference and a CUDA backend (one thread per (column, LP) and per (row, LP), the matrix read once per warp; explicit round-to-nearest intrinsics, and -ffp-contract=off on the CPU file, so the two return identical duals; restart statistics as fixed-chunk partial sums added on the host) | Chambolle & Pock, *A first-order primal-dual algorithm for convex problems with applications to imaging*, JMIV 40 (2011); Applegate et al., *Practical large-scale linear programming using primal-dual hybrid gradient*, NeurIPS 2021 (restarts, primal weight, preconditioning); Lu & Yang, *cuPDLP.jl*, arXiv:2311.12180 (2023) (KKT restart test, constant step); Neumaier & Shcherbina (2004) (the bound); arXiv:2601.21990 (2026) (batched first-order strong branching, the use) | `src/pdhg/batch_pdhg.cpp`, `src/pdhg/batch_pdhg_cpu.cpp`, `src/gpu/pdhg_batch.cu`, `src/mip/branch_and_bound_batch.cpp` |
| Multi-GPU restarted PDHG by row blocks (#295, `gpu_devices` with two or more ids): contiguous row blocks balanced by nonzeros plus rows, the primal iterate replicated and the dual partitioned; A^T y summed across the cards by an all-gather of the per-card partials (`cudaMemcpyPeerAsync` over P2P where every pair allows it and `gpu_peer_access` is true, else staged through pinned host memory) and a sum in slot order on every card; movement and interaction reduced per block then in block order (no `atomicAdd`), both products cuSPARSE `CSR_ALG2` on explicitly stored A_k and A_k^T; the run is bitwise reproducible and the same for either transport | Chambolle & Pock (2011) and Applegate et al. (PDLP, 2021) as above; Lu & Yang, arXiv:2311.12180, for the device layout; Saad, *Iterative Methods for Sparse Linear Systems*, 2nd ed., SIAM (2003), ch. 11, for the row-block distributed product | `src/gpu/pdhg_multi_gpu.cu`, `src/gpu/pdhg_multi_gpu_device.cu`, `src/gpu/multi_gpu_exchange.cu`, `src/gpu/multi_device.cpp` (`partition_rows_by_nonzeros`) |
| Ruiz equilibration | Ruiz, *A scaling algorithm to equilibrate both rows and columns norms in matrices*, RAL-TR-2001-034 | `src/la/scaling.cpp` |
| Cut efficacy (violation over the Euclidean norm) as the selection test, and the support floor on the density cap (`cut_efficacy_test`, `cut_support_floor`, #496) | Wesselmann & Suhl, *Implementing cutting plane management and selection techniques*, University of Paderborn, 2012; Achterberg, *Constraint Integer Programming*, PhD thesis, TU Berlin, 2007, section 8.10 | `src/mip/cuts.cpp` |
| Row-parallel sparse matrix-vector product (CSR gather, one output per thread, static partition; `pdhg_parallel_spmv`, #487) | Saad, *Iterative Methods for Sparse Linear Systems*, 2nd ed., SIAM 2003, section 3.5 | `src/pdhg/pdhg.cpp`, `src/la/sparse.cpp` |
| Parallel PDHG vector updates with thread-count-independent sums (`pdhg_parallel_updates`, #487): the projected primal and dual updates, the extrapolation and the running sums over a static partition; the step rule's movement and interaction sums in fixed chunks of `kPdhgParallelChunk` entries, chunk totals added in chunk order, so the bits do not depend on the thread count | Higham, *Accuracy and Stability of Numerical Algorithms*, 2nd ed., SIAM 2002, section 4.1 (summation order); the iteration itself Chambolle & Pock (2011) and Applegate et al. (PDLP, 2021) as above | `src/pdhg/pdhg_parallel.cpp` |
| Two sparse products per PDHG iteration instead of three (`pdhg_two_matvec`, #479; the default `cpu` takes two products on the CPU engine and keeps three on the CUDA engine, from the A/B `bench/results/pdhg-two-matvec-58a8374.csv`: CPU per-iteration time 0.74 to 0.84 of three products on 14 of 14 instances, the device 0.62 to 1.26, slower on kkt_1000x1000 and kkt_5000x5000; `true` takes two on both, `false` three on both): A x_k is carried between iterations, A x_{k+1} is computed once, and A xbar = 2 A x_{k+1} - A x_k and A dx = A x_{k+1} - A x_k are derived by linearity; the cache is recomputed at every restart. Held iterate by iterate against the three-product path in `tests/unit/test_pdhg_two_matvec.cpp` (lockstep window, and the cache bitwise A x_k at every iterate of 10,000) | Chambolle & Pock, JMIV 40(1), 2011, Algorithm 1 (the extrapolation xbar = 2 x_{k+1} - x_k); Applegate et al. (PDLP), NeurIPS 2021, section 3.1 (the interaction term (y' - y)' A (x' - x) of the step-size rule); the derivation itself is the linearity of A | `src/pdhg/pdhg.cpp`, `src/gpu/pdhg_gpu.cu` (`k_derive_products`), `src/gpu/pdhg_graph.cu` |
| Diagonal preconditioning, alpha = 1 | Pock & Chambolle, *Diagonal preconditioning for first order primal-dual algorithms*, ICCV 2011, section 4 | `src/la/scaling.cpp` |
| Moreau decomposition for the support-function prox | Rockafellar, *Convex Analysis*, theorem 31.5 | `src/pdhg/pdhg.cpp` |
| Branch and bound | Land & Doig, *An automatic method of solving discrete programming problems*, Econometrica 28(3), 1960; Wolsey, *Integer Programming*, ch. 7 | `src/mip/branch_and_bound.cpp` |
| Node propagation from row activities | Savelsbergh, *Preprocessing and probing for MIP*, ORSA J. Computing 6(4), 1994 | `src/mip/branch_and_bound.cpp` |
| Search shape: propagation at nodes, incumbent as cutoff | Achterberg, *Constraint Integer Programming* (thesis, 2007), ch. 5–6 | `src/mip/branch_and_bound.cpp` |
| Hyper-sparse back-substitution through U in the FTRAN, U stored by column as well as by row, zero results skipped; the row-wise gather kept as the reference the push form is tested against (#68) | Gilbert & Peierls, *Sparse partial pivoting in time proportional to arithmetic operations*, SIAM J. Sci. Stat. Comput. 9 (1988) | `src/la/lu.cpp` (`solve`, `solve_reference`) |
| Hyper-sparse FTRAN and BTRAN by symbolic reach (#464; `lu_hyper_sparse`, off by default until its A/B): a breadth-first search over the graphs of L and U (FTRAN) or of U^T and L^T (BTRAN) from the right-hand side's nonzeros finds every step the result can reach, and the numeric passes run over those steps only, sorted into the order the full loops use so the results are identical to the bit; a right-hand side or a reach above 10% of the rows takes the full loops. Every solve's result density is binned and reported at verbose level and to the profiler | Gilbert & Peierls, *Sparse partial pivoting in time proportional to arithmetic operations*, SIAM J. Sci. Stat. Comput. 9(5) (1988); Hall & McKinnon, *Hyper-sparsity in the revised simplex method and how to exploit it*, Computational Optimization and Applications 32 (2005) | `src/la/lu_hyper.cpp`, `src/simplex/simplex_report.cpp` |
| Sparse symmetric LDL^T: approximate minimum degree ordering on the quotient graph (#193), elimination tree, symbolic pattern once, up-looking numeric factorization with diagonal regularization (#70) | Davis, *Direct Methods for Sparse Linear Systems*, SIAM (2006), ch. 4; Liu, *The role of elimination trees in sparse factorization*, SIAM J. Matrix Anal. Appl. 11 (1990); Tinney & Walker, Proc. IEEE 55 (1967); Amestoy, Davis & Duff, *An approximate minimum degree ordering algorithm*, SIAM J. Matrix Anal. Appl. 17 (1996); Altman & Gondzio, Optim. Methods Softw. 11 (1999) | `src/la/ldl.cpp` |
| Supernodal numeric factorization for the sparse LDL^T (#470, option `ipm_supernodal`, off by default; first slice, sequential): fundamental supernodes from the elimination tree and the column counts, a left-looking supernodal factorization whose updates are dense products over each source supernode's contiguous columns and whose diagonal blocks are factored by a dense right-looking LDL^T under the scalar path's pivot floor, all kernels written here (no vendor BLAS); the factor is written back into the scalar layout, the scalar up-looking path is the oracle | Liu, Ng & Peyton, *On finding supernodes for sparse matrix computations*, SIAM J. Matrix Anal. Appl. 14 (1993); Ng & Peyton, *Block sparse Cholesky algorithms on advanced uniprocessor computers*, SIAM J. Sci. Comput. 14 (1993); Liu, SIAM J. Matrix Anal. Appl. 11 (1990); Davis, *Direct Methods for Sparse Linear Systems*, SIAM (2006), ch. 4; Altman & Gondzio, Optim. Methods Softw. 11 (1999) | `src/la/ldl_supernodal.cpp` |
| Interior point normal equations factored on the device by NVIDIA cuDSS (#489, `ipm_linear_solver=cudss`, off by default): analysis once per pattern, numeric LDL^T every iteration with pivoting off and a static pivot epsilon equal to the CPU factor's regularization, a factor holding a negative pivot redone on the CPU, the Newton iteration and the iterative refinement unchanged on the host | Shin et al., *MadIPM GPU interior point*, arXiv:2508.16094 (paper only); NVIDIA cuDSS documentation. Licence: judgement call 28 in section 5 | `src/gpu/cudss_factor.cpp`, `src/ipm/ipm.cpp` |
| Primal-dual interior-point method, Mehrotra predictor-corrector on the normal equations of the bounded-variable form; no basis, no infeasibility certificate (#56; `--option algorithm=ipm`) | Mehrotra, SIAM J. Optim. 2 (1992); Wright, *Primal-Dual Interior-Point Methods*, SIAM (1997), ch. 10–11; Altman & Gondzio (1999) for the regularization | `src/ipm/ipm.cpp` |
| Proximal primal-dual regularization of the LP interior point (#473, option `ipm_proximal_regularization`, off by default): the regularized augmented system `[-(Theta^-1 + rho I), A'; A, diag(theta_s) + delta I]` (logicals eliminated first) in place of the normal equations and their fixed 1e-10 floor, quasi-definite for any rho, delta > 0 and factored by the signed LDL^T the QP interior point uses (`SparseLdl::factorize_quasidefinite`), rho = delta following mu down to 1e-8 and raised x100 when a pivot is lifted; the unregularized Newton system solved from those factors by iterative refinement, a correction kept only while the unregularized residual falls; a solve whose residual relative to the larger of 1 and the right-hand side's infinity norm stays above 1e-8 is counted, reported, and shrinks rho for the next factorization; a non-finite direction raises the rho floor x100 per recovery to a cap of 1e-4 | Altman & Gondzio, *Regularized symmetric indefinite systems in interior point methods for linear and quadratic optimization*, Optim. Methods Softw. 11 (1999); Friedlander & Orban, *A primal-dual regularized interior-point method for convex quadratic programs*, Math. Program. Comput. 4 (2012); Vanderbei, *Symmetric quasidefinite matrices*, SIAM J. Optim. 5(1) (1995); Higham, *Accuracy and Stability of Numerical Algorithms*, 2nd ed., SIAM (2002), ch. 12 | `src/ipm/proximal_system.cpp`, `src/ipm/ipm.cpp` (`factorize`, `proximal_newton_direction`) |
| Gondzio's multiple centrality correctors in the LP interior point (#472, option `ipm_centrality_correctors`, 0 = off): after the Mehrotra direction, from a trial point at the aspiration step min(1.5 alpha + 0.3, 1) the complementarity products outside [0.1, 10] sigma mu are pulled back towards that band (a large one by at most 10 sigma mu) through one more solve with the same factors; a corrector is kept only when its solve is finite and accurate (dense-column or n x n conjugate gradients converged, proximal refinement met its target, both judged per corrector and a refused one leaving the loop's flags as they were), it lengthens alpha_p + alpha_d by 1%, and at the point the step reaches neither the worst product in the stopping test's measure nor the spread (largest product over the mean) grows and the floor (smallest product over the mean) does not fall, the first refusal ending that iteration's correctors; the number tried is capped by the option and grows with the factor's estimated factorization-to-solve cost ratio, never with a clock, and is at most 1 on the dense-column and n x n paths and at most 2 on the proximal path (whose ratio is divided by its refinement's solves); off, the iteration is the one without it | Gondzio, *Multiple centrality corrections in a primal-dual method for linear programming*, Computational Optimization and Applications 6 (1996); Colombo & Gondzio, *Further development of multiple centrality correctors for interior point methods*, Computational Optimization and Applications 41 (2008) | `src/ipm/centrality.cpp`, `src/ipm/ipm.cpp` (`centrality_correctors`) |
| Crossover from the interior point to a vertex (#219): classify each column and row of the interior point's answer as at-bound (within a relative tolerance, reduced cost agreeing) or interior, take the m interior entries with the most slack as a basis guess, warm-start the dual simplex from it and keep its vertex when it reaches optimal; the guess is a heuristic and nothing rests on it, the simplex proves the vertex | Bixby, *Solving real-world linear programs: a decade and more of progress*, Operations Research 50 (2002), sec. 4; Andersen & Ye, *Combining interior-point and pivoting algorithms for linear programming*, Management Science 42 (1996) | `src/simplex/crossover.cpp` |
| The primal push of the crossover (#343): every nonbasic variable starts at the interior point's value and is pushed to its nearer bound along `B^-1 a_k` with a ratio test against the basic variables, a blocking basic leaving the basis, so the point stays feasible and the primal loop finishes from a vertex of the optimal face | Bixby, Operations Research 50 (2002), sec. 4; Andersen & Ye, Management Science 42 (1996); Megiddo, *On finding primal- and dual-optimal bases*, ORSA J. Computing 3 (1991) | `src/simplex/simplex_push.cpp` (`Simplex::run_push`) |
| Crossover from an interior point that is not optimal (#474, option `crossover_from_nonoptimal`, off by default): a `feasible`, time-limited or iteration-limited answer, or the best iterate the interior point attaches to a numerical error, is handed to the same guess, push and primal loop when its scaled primal and dual infeasibility are at most `tol::kCrossoverStartInfeasibility` (1e-4); only the simplex vertex, measured by the status guard, may be reported optimal, and an iterate nothing used is withdrawn from the numerical error | Megiddo, *On finding primal- and dual-optimal bases*, ORSA J. Computing 3(1) (1991); Bixby & Saltzman, *Recovering an optimal LP basis from an interior point solution*, Operations Research Letters 15 (1994) | `src/simplex/crossover.cpp` (`crossover_when_wanted`, `crossover_start_is_usable`), `src/ipm/ipm.cpp` (`finish`) |
| Verifier-gated concurrent engine race under `algorithm=auto` (#476, option `engine_race`, off by default): the dual simplex, the interior point with crossover and PDHG run at once, each through its own presolve and postsolve, on its own thread, logger and out-of-memory guard; an answer wins only when an in-process KKT check against the original model at the project tolerances passes (or, for an infeasibility, its Farkas certificate checks out), a rejected answer neither wins nor stops the others, the first accepted one cancels the rest; deterministic mode or one core runs the same engines in a fixed order | Concurrent optimization as documented in the IBM ILOG CPLEX User's Manual ("Concurrent optimizer") and the Gurobi Reference Manual (parameter `Method` = 3 and 4) - user-facing behaviour only; the KKT conditions of LP, Nocedal & Wright, *Numerical Optimization*, 2nd ed. (2006), thm. 13.1 | `src/core/engine_race.cpp` |
| Engine-selection features and a learned choice (#477, tooling only, not wired into `algorithm=auto`): m, n, nnz, density, the longest column, a dense-column count (more than `kIpmDenseColumnFactor` sqrt(m) entries, the interior point's own test), rows per column, equality-row, integer, boxed, free and fixed shares, and a pattern-only upper bound on nnz(A A^T), printed by `sankhya info --features` with the rule table's choice; our own CART (Gini impurity, depth 4, at least 3 rows per leaf, deterministic ties) trained on generated instances disjoint from every reported set and split by shape (every seed of a held-out shape is held out, one shape per generator), emitting readable C++ only when at least 60 training and 20 held-out rows from at least 8 and 4 distinct shapes exist, no shape is on both sides of the split, and the tree loses less time than the rule table on the held-out rows | Rice, *The algorithm selection problem*, Advances in Computers 15 (1976); Kotthoff, *Algorithm selection for combinatorial search problems: a survey*, AI Magazine 35(3) (2014); Breiman, Friedman, Olshen & Stone, *Classification and Regression Trees* (1984); Andersen, Gondzio, Meszaros & Xu, *Implementation of interior point methods for large scale linear programming*, in Terlaky (ed.), Interior Point Methods of Mathematical Programming, Kluwer (1996) (dense columns) | `src/core/engine_features.cpp`, `bench/runners/engine_selection_data.py`, `bench/runners/engine_selection_cart.py` |
| PDHG infeasibility/unboundedness detection from restart iterate differences (#484, option `pdhg_detect_infeasibility`, off by default): at each restart, the period's iterate difference (already computed for the primal-weight update, at no extra cost when this is off) is handed directly to this project's own certificate checker - `ray_proves_unbounded`/`farkas_proves_infeasible`, the same functions every other engine's proof is held to - rather than re-deriving the paper's own numerical thresholds; accepted only when the checker confirms the direction, so a wrong guess declines and cannot lie. Measured on 29 Netlib infeasible instances with presolve off (isolating PDHG's own detection): 5 of 29 detected with a certificate `tools/verify_solution.py` accepts within 60s (`bgdbg1`, `galenet`, `itest2`, `itest6`, `mondou2` - all small); the rest hit the time limit without a confirmable ray, an honest limitation of first-order detection speed the cited paper itself notes, not a false claim either way. Confirmed to never falsely claim infeasible/unbounded over the nine committed optimal Netlib instances (`tests/unit/test_pdhg_infeasibility_detection.cpp`) | Applegate, Diaz, Lu & Lubin, *Infeasibility detection with primal-dual hybrid gradient for large-scale linear programming*, SIAM J. Optim. 34(1) (2024) (arXiv 2102.04592) | `src/pdhg/pdhg.cpp` (`detect_certificate_from_restart`) |
| Exact rational verification of a reported optimal LP basis (#521, option `exact`, off by default): the basis a double-precision LP engine reports rebuilt in exact rational arithmetic over `__int128` - every model coefficient converted bit-exactly from the double the reader already produced, never rounded - and checked for exact primal and dual feasibility by a dense exact Gaussian elimination; declines (never a false verdict) on an `__int128` overflow, a basis larger than its row cap, an integer column or a quadratic objective. A narrower, self-contained piece of the cited papers' pipeline: it checks the ONE basis double precision found rather than iteratively refining toward a better one. Measured: naive rational elimination over `__int128` overflows on almost any realistic decimal-coefficient model, even a 3-row one (`demo/crude_blend.mps` declines) - it verifies correctly on integer-coefficient LPs (`tests/unit/test_exact_verify.cpp`, including two hand-built bases proven infeasible/singular to confirm rejection, not rubber-stamping) but a wider arithmetic backing than `__int128` would be needed before this verifies most real instances | Gleixner, Steffy & Wolter, *Iterative refinement for linear programming*, INFORMS J. Computing 28(3) (2016); Gleixner & Steffy, *Linear programming using limited-precision oracles*, Math. Programming 183 (2020) | `src/exact/rational.hpp`, `src/exact/exact_verify.cpp` |
| In-process KKT check of an LP optimum, a C++ reimplementation of the checks `tools/verify_solution.py` makes (bounds and recomputed row activities relative to their terms, activity and objective agreement, reduced costs against c - A^T y, multiplier signs, complementary slackness, the basis, strong duality with per-item accounting) so the race can judge an answer inside the process while the Python verifier stays independent and never links the solver | Nocedal & Wright, *Numerical Optimization*, 2nd ed. (2006), thm. 13.1; the scalings are those argued in `tools/verify_solution.py` | `src/core/kkt_check.cpp` |
| Dense columns in the interior point's normal equations (#467, option `ipm_dense_columns`, off by default): columns with more than `ipm_dense_column_factor` * sqrt(m) entries left out of A Theta A^T, the rank-k correction applied by Sherman-Morrison-Woodbury with the scaled k x k Schur complement I + V^T M_s^-1 V, used as the preconditioner of conjugate gradients on the whole system (the sparse factor alone when the Schur complement is not positive definite), stopped on the normwise backward error; and the size of A Theta A^T counted from the pattern of A before assembly (largest column clique, sum of cliques, exact marker count with early exit), refused over the factor budget | Andersen, Gondzio, Meszaros & Xu, *Implementation of interior point methods for large scale linear programming*, in Terlaky (ed.), Interior Point Methods of Mathematical Programming, Kluwer (1996), sec. 5; Wright, *Primal-Dual Interior-Point Methods*, SIAM (1997), ch. 11; Hestenes & Stiefel, J. Res. Nat. Bur. Standards 49 (1952); Golub & Van Loan, *Matrix Computations*, 4th ed. (2013), sec. 11.5; Higham, *Accuracy and Stability of Numerical Algorithms*, 2nd ed. (2002), sec. 7.1; Davis, *Direct Methods for Sparse Linear Systems*, SIAM (2006), sec. 2.8 | `src/ipm/dense_columns.cpp`, `src/la/normal_pattern.cpp`, `src/ipm/ipm.cpp` (`factorize`, `solve_normal`) |
| The interior point's normal equations on the n x n side (#469, option `ipm_normal_side` = rows / columns / auto, default rows): N = Theta^-1 + A^T D^-1 A factored by the same sparse LDL^T, M dy = r solved by conjugate gradients on M preconditioned with the Woodbury identity M^-1 = D^-1 - D^-1 A N^-1 A^T D^-1 on N's factors, D floored at 1e-8 of each row's A Theta A^T diagonal in the preconditioner, an unconverged solve handled as an unconverged dense-column solve is; auto counts both lower triangles from the pattern of A with early exit (#467's count) and orders both, keeping the smaller symbolic factor, or, when the rows do not outnumber the columns, takes the column side only when the row side's count is over the factor budget; the choice runs under the set-up share; not combined with the proximal or dense-column paths | Woodbury, *Inverting modified matrices*, Memorandum Report 42, Statistical Research Group, Princeton (1950); Golub & Van Loan, *Matrix Computations*, 4th ed. (2013), sec. 2.1.4 and 11.5; Wright, *Primal-Dual Interior-Point Methods*, SIAM (1997), ch. 11; Zanetti & Gondzio, arXiv:2508.04370 (2025), for choosing the system by predicted factor size; Higham, *Accuracy and Stability of Numerical Algorithms*, 2nd ed. (2002), sec. 7.1 | `src/ipm/column_side.cpp` (`ColumnSide::choose`), `src/ipm/ipm.cpp` (`choose_side`, `solve_normal`) |
| Dual purification at the interior point's finish (#209): the least-squares correction of y over the strictly interior variables, `dy = (A_I A_I^T + eps I)^-1 A_I (c_I - A_I^T y)`, through the same normal equations, accepted only if the interior reduced costs shrink and no sign condition breaks - the dual half of a crossover, without the primal half | Bixby, *Solving real-world linear programs: a decade and more of progress*, Operations Research 50 (2002), sec. 4; Andersen & Ye, *Combining interior-point and pivoting algorithms for linear programming*, Management Science 42 (1996) | `src/ipm/ipm.cpp` (`purify_duals`) |
| Reliability branching: pseudocosts, strong branching on unreliable candidates with capped warm-started dual probes, product score (#69; `--option mip_branching=most-fractional` keeps the old rule) | Achterberg, Koch & Martin, *Branching rules revisited*, Operations Research Letters 33 (2005), 42–54 | `src/mip/branch_and_bound.cpp` |
| Node LP factor reuse (#501; `mip_node_factor_cache`, off by default until its A/B): the LU of a node LP's starting basis, kept after a first factorization that needed neither the Markowitz ladder nor a repair, keyed by the scaled matrix's id and the basis in seed order, and handed to the next node LP (a strong-branching probe, a child) that starts from the same basis; the copy is the factorization that solve would compute, so the tree is unchanged, node for node | Achterberg, *Constraint Integer Programming*, PhD thesis, TU Berlin (2007), on warm-starting node LPs from the parent's basis; Koberstein, *The dual simplex method, techniques for a fast and stable implementation*, PhD thesis, Paderborn (2005), on bound changes in the bounded dual simplex | `src/simplex/factor_cache.cpp` |
| Iterative refinement of the final basis, primal and dual, residual in compensated arithmetic (#72) | Wilkinson, *Rounding Errors in Algebraic Processes* (1963), ch. 4; Ogita, Rump & Oishi, *Accurate sum and dot product*, SIAM J. Sci. Comput. 26 (2005) | `src/simplex/primal_simplex.cpp` (`refine_final_basis`) |
| Cost perturbation on a dual-degenerate stall, nonbasic costs only so dual feasibility is preserved by construction | Maros, *Computational Techniques of the Simplex Method*, ch. 9 (the bound-shifting scheme, applied to the dual's costs); Koberstein (2005), above, sec. 6.2 | `src/simplex/dual_ratio.cpp` |
| Dual simplex cost perturbation at the start (#465; `dual_perturb_costs_at_start`, off by default until its A/B): when the caller's structural costs take fewer than n/4 distinct values, every nonbasic structural cost moves by (1e-5 + 1e-5 \|c_j\|) times a deterministic per-column factor in [0.5, 1], in the direction that keeps the basis dual feasible; the exact costs are restored at the end and the primal simplex finishes. Taken from the thesis text only (judgement call 12) | Koberstein, *The dual simplex method, techniques for a fast and stable implementation*, PhD thesis, Paderborn (2005), ch. 6; Maros (2003), ch. 9 | `src/simplex/dual_ratio.cpp` |
| Harris two-pass step inside the dual bound-flipping ratio test, with cost shifting (#465; `dual_ratio_test=harris`, off by default until its A/B): breakpoints grouped under the tightest breakpoint with every reduced cost relaxed by a tenth of the dual tolerance, a boxed group the slope can pay for flipped whole, the largest pivot of the first group it cannot pass entering; an entering reduced cost on the wrong side of zero is removed by shifting its cost, and every shift is undone before an answer leaves the dual loop | Harris, *Pivot selection methods of the Devex LP code*, Math. Programming 5 (1973); Koberstein (2005), above, ch. 6; Maros (2003), ch. 10 | `src/simplex/dual_ratio.cpp` |
| Robustness suite: the classic cycling examples, adversarial families judged by the exact oracle, and the conditioning / near-parallel / cost-ratio / redundancy / degeneracy sweeps with an optimum known by construction (#71) | Beale, *Cycling in the dual simplex algorithm*, Naval Research Logistics Quarterly 2 (1955); Kuhn's example as given in Chvátal, *Linear Programming* (1983), ch. 3; the KKT construction is the oracle fuzz's (above) | `tests/robustness/test_robustness.cpp`, `bench/runners/robustness.py` |
| Solution pool: every integer-feasible point the search finds, deduplicated by integer assignment, bounded and best first; `pool_complete` continues past integral nodes (three-way split on an unfixed integer column) and prunes on the pool's worst member so the pool provably holds the k best; `pool_diversity` evicts by nearest Hamming distance (#225) | Danna, Fenelon, Gu & Wunderling, *Generating multiple solutions for mixed integer programming problems*, IPCO 2007, LNCS 4513; Glover, Lokketangen & Woodruff, *Scatter search to generate diverse MIP solutions*, in *Computing Tools for Modeling, Optimization and Simulation* (2000) | `src/mip/solution_pool.cpp`, `src/mip/branch_and_bound.cpp` |
| Feasibility pump whose L1 projection is solved by restarted PDHG, on the CUDA device when there is one (#509; `gpu_pump`, off by default, at the MIP root): the projection LP is built once - two-valued integer columns by a +/-1 cost, general integers by one auxiliary column and two rows each - and only its costs and row sides move per round; a repeated rounding flips T/2..3T/2 columns, a cycle within the last R rounds is restarted; the continuous columns are completed by an LP with the integer columns fixed, and every point is checked against the model's rows and bounds before the search checks it again | Fischetti, Glover & Lodi, *The feasibility pump*, Mathematical Programming 104 (2005); Bertacco, Fischetti & Lodi, *A feasibility pump heuristic for general mixed-integer problems*, Discrete Optimization 4 (2007); Mexi et al., arXiv:2307.03466; Corduk et al., arXiv:2510.20499 | `src/mip/pdhg_heuristics.cpp` |
| Fix-and-propagate (#509; `gpu_fix_and_prop`, off by default, at the MIP root): integer columns fixed least fractional first to the integer nearest the relaxation inside the current domain, in batches that double on success and halve on a proved-empty box, with the #510 synchronous propagator (CUDA or CPU, identical bounds) after each batch; a bounded number of back-ups; completion LP for the continuous columns; Feasibility Jump repair | Gamrath, Berthold, Heinz & Winkler, *Structure-based primal heuristics for mixed integer programming*, Optimization in the Real World, Springer (2016); Corduk et al., arXiv:2510.20499 | `src/mip/pdhg_heuristics.cpp`, `src/mip/domain_propagation.cpp`, `src/gpu/domain_prop.cu` |
| Feasibility Jump (#506), MILP primal heuristic, off by default (`mip_heur_fj`): weighted local search over the original rows with no LP; each column's jump value is the exact minimiser of its convex piecewise-linear weighted violation, found by sorting the breakpoints; the best of 25 sampled jumps is taken; at a local minimum every violated row's weight gains 1 (the objective's, when none is violated). Runs before the root LP and from the rounded root relaxation; every point goes through offer_incumbent() | Luteberget and Sartor, *Feasibility Jump: an LP-free Lagrangian MIP heuristic*, Mathematical Programming Computation 15 (2023); written from the paper (see judgement call 17) | `src/mip/feasibility_jump.cpp`, `src/mip/branch_and_bound_heuristics.cpp` |
| GPU Feasibility Jump (#508), off by default (`gpu_feasibility_jump`, where `mip_heur_fj` runs FJ): the CPU search above, one per CUDA block (two blocks per multiprocessor, each its own seed): the point, activities, row weights and violated list on the device; each of a block's 8 warps scores 4 sampled columns, lanes striding over the column's nonzeros with shuffle reductions; the first breakpoint where the slope turns non-negative is found by a randomised selection over the breakpoints (least-hash pivot, one reduction for the slope right of it) instead of a sort; a two-valued integer column scores its flip alone; the move updates the activities in parallel; the weight update at a local minimum as in the paper. No atomics, counter-based hashing for every draw, so a run is reproducible for a seed. Every point is verified on the host against the original model (`feasibility_jump_point_is_feasible`) before it is handed to offer_incumbent(); without a device the CPU search runs | Luteberget and Sartor, *Feasibility Jump: an LP-free Lagrangian MIP heuristic*, Mathematical Programming Computation 15 (2023); Corduk, Sielski, Boucher & Aatish, *GPU-accelerated primal heuristics for mixed integer programming*, arXiv:2510.20499; written from the papers and the CPU reference, no solver source read | `src/gpu/gpu_fj.cu`, `src/gpu/gpu_fj_device.cuh`, `src/mip/branch_and_bound_heuristics.cpp` |
| Local-MIP improvement heuristic (#507, `mip_local_mip`, off by default): after finding an incumbent, run lift moves (shift an integer variable ±1 in the objective-improving direction, provided all row activities stay feasible), tight/breakthrough moves (at an infeasible working point, the move with the greatest reduction in weighted constraint violations, where a virtual objective-cutoff row `c^T x ≤ z*−1` is added with the same FJ weights), a tabu list of (variable, direction) pairs with tenure 3+rand(10), and multiplicative weight updates with additive smoothing decay | Lin, Zou and Cai, *Local-MIP: efficient local search for mixed integer programming*, CP 2024, LIPIcs 307 | `src/mip/heuristics.cpp` (`local_mip_improve`), `src/mip/branch_and_bound_heuristics.cpp` |
| Conflict analysis (#292, off by default): from a node proved infeasible, the subset of its branching decisions that the rows and the global bounds cannot satisfy, re-proved from the global bounds by propagation or by the node LP's Farkas multipliers evaluated conservatively, shrunk by a bounded deletion filter, stored as a bound disjunction and used in node propagation to prune and to fix bounds | Achterberg, *Conflict analysis in mixed integer programming*, Discrete Optimization 4(1) (2007); Witzig, Berthold & Heinz, *Experiments with conflict analysis in mixed integer programming*, CPAIOR 2017; Junker, *QuickXplain*, AAAI 2004 | `src/mip/conflict.cpp`, `src/mip/branch_and_bound_conflicts.cpp` |
| Conflict analysis from cutoff nodes (#503, `conflict_cutoff`, off by default, needs `conflict_analysis`): from a node pruned by bound, after it is left, the subset of its branching decisions over which the node LP's row duals prove, from the global bounds, that no point reaches the cutoff (incumbent less `mip_absolute_gap`): by propagation, or by the Neumaier-Shcherbina safe bound of those duals over the literal box exceeding the cutoff; the same deletion-filter minimisation as for infeasible nodes; the cutoff is stored with the conflict and exported. Not for MIQP or `pool_complete` | Achterberg, *Conflict analysis in mixed integer programming*, Discrete Optimization 4(1) (2007); Witzig, Berthold & Heinz, *Experiments with conflict analysis in mixed integer programming*, CPAIOR 2017; Neumaier & Shcherbina, *Safe bounds in linear and mixed-integer linear programming*, Math. Programming 99 (2004) | `src/mip/branch_and_bound_conflicts.cpp`, `src/mip/conflict.cpp`, `tests/unit/test_conflict_cutoff.cpp` |
| Safe LP bounds (#519, option `safe_bounds`, off by default): a lower bound on the LP relaxation from ANY row multipliers y, by weak duality for the Lagrangian with y fixed (row sides priced by the sign of y, reduced costs c - A'y priced over the column box), computed in interval arithmetic by round-to-nearest plus a one-ulp outward step, with exact sums detected by TwoSum; a multiplier on a missing row side is set to zero, and a column whose reduced cost needs a missing bound gets one implied by a single row, else the bound is -inf. Branch and bound prunes and orders on it and logs the largest believed-minus-safe gap | Neumaier & Shcherbina, *Safe bounds in linear and mixed-integer linear programming*, Math. Programming 99 (2004) 283-296; Knuth, *The Art of Computer Programming* vol. 2, sec. 4.2.2 (TwoSum); Savelsbergh, *Preprocessing and probing for mixed integer programming problems*, ORSA J. Computing 6(4) (1994) (row-implied bounds) | `src/core/safe_bound.cpp`, `src/mip/branch_and_bound_safe.cpp` |
| MILP optimality certificates (#518, option `write_certificate`, off by default): the branch-and-bound tree written as a VIPR derivation - each leaf's bound is the safe bound (#519) of its LP duals over the box its branching assumptions define, with the row-implied column bounds it uses derived as steps of their own, an infeasible leaf a contradiction from its Farkas multipliers (re-solved by the primal simplex when the dual's does not prove it exactly), each branching node the unsplit of its children's bounds over x <= d or x >= d + 1, and a final Chvatal-Gomory rounding when the objective is integral; every number is the exact decimal of the double. Checked by an independent Python checker in exact rational arithmetic (`fractions.Fraction`), with one stated extension to the format (a lin step is completed over the column bounds) | Cheung, Gleixner & Steffy, *Verifying integer programming results*, IPCO 2017, LNCS 10328 (the VIPR format and its reasons); Eifler & Gleixner, *A computational status update for exact rational mixed integer programming*, Math. Programming 197 (2023); Neumaier & Shcherbina (2004) for the leaf bounds; Chvatal, *Edmonds polytopes and a hierarchy of combinatorial problems*, Discrete Math. 4 (1973) (rounding) | `src/mip/certificate_writer.cpp`, `src/mip/branch_and_bound_certificate.cpp`, `tools/verify_certificate.py`, `tools/verify_certificate_model.py` |
| Cut rows in MILP certificates (#518, within `write_certificate`): every root cut the node LPs carry is derived from the model before any leaf uses it. Knapsack covers (one row, the multiplier minimising the rounded bound over the row's breakpoints) and {0,1/2}-cuts (multipliers 1/2) as Chvatal-Gomory `rnd` steps; Gomory mixed-integer cuts (the tableau row's disjunction, the nonnegativity of each nonbasic logical weighted by what the cut's coefficient exceeds the tableau's) and MIR cuts (the aggregation's rows, the divisor's disjunction) as splits: two assumptions, a `lin` under each and the `uns` that discharges them. The generator only proposes multipliers; the right-hand side each side proves is computed with the safe bound (outward rounding) over the model's column box, a cut it does not reach is dropped, one it reaches up to `kCertificateCutSlack` gets that right-hand side before it becomes a row. Clique, flow cover, implied-bound and c-MIR cuts state no derivation and are not separated in this mode; tree cut rounds are off | Cheung, Gleixner & Steffy, IPCO 2017 (sec. 3: CG cuts as `rnd`, split cuts through `asm`/`uns`); Chvatal (1973); Nemhauser & Wolsey, *A recursive procedure to generate all cuts for 0-1 mixed integer programs*, Math. Programming 46 (1990) and Cornuejols, *Valid inequalities for mixed integer linear programs*, Math. Programming 112 (2008), sec. 5 (GMI and MIR as split cuts); Caprara & Fischetti, *{0,1/2}-Chvatal-Gomory cuts*, Math. Programming 74 (1996); Neumaier & Shcherbina (2004) | `src/mip/cut_derivation.cpp`, `src/mip/certificate_cuts.cpp`, `tools/test_verify_certificate_cuts.py` |
| Restarted, reflected Halpern iteration and a PID primal weight in the first-order QP engine (#493, CPU; options `qp_halpern`, off by default, and `qp_primal_weight_pid`, on by default since the Maros-Meszaros A/B `bench/results/maros-meszaros-493-{off,pid,both}-58a8374.csv`: 19 of 138 pass against 2 off, `hs76` lost; Halpern with it passes 16): z <- w((1+rho) T z - rho z) + (1-w) z_anchor, w = (k+1)/(k+2), on the Condat-Vu step T (Q x once per iteration); the reflection rho kept below delta - 1 = (1/tau - L/2)/(1/tau + L/2) from Condat's relaxation bound; restart when ||T z - z|| falls to 0.2 of the period's first or after 0.36 of all iterations; convergence measured and the answer reported at T z; at each restart log omega -= kp e + ki sum(e) + kd de on e = log(omega ||dx|| / ||dy||), and tau, sigma recomputed from omega = sqrt(sigma/tau) under the step condition. The GPU QP operator the issue also names is not implemented | Lu & Yang, *Restarted Halpern PDHG for linear programming*, arXiv:2407.16144; Lu & Yang, *A practical and optimal first-order method for large-scale convex quadratic programming* (PDQP), arXiv:2311.07710; Lu, Peng & Yang, *cuPDLPx*, arXiv:2507.14051 (PID primal weight); Applegate et al., NeurIPS 2021, sec. 3.2 (primal weight); Halpern, Bull. AMS 73 (1967); Condat, JOTA 158 (2013), Theorem 3.1 | `src/qp/qp_first_order_accel.cpp`, `src/qp/qp_condat_vu.cpp` |
| MIQP: branch and bound over convex QP node relaxations | Gupta & Ravindran, *Branch and bound experiments in convex nonlinear integer programming*, Management Science 31(12), 1985 | `src/mip/branch_and_bound.cpp` |
| MIQP node IPM (#494, `miqp_node_ipm`, off by default): the proximal QP interior point (#490) as the node relaxation solver, capped at its own 200-iteration ceiling; each node pruned on a bound valid for any primal point p and row duals y: the convex objective linearised at p (F(x) >= F(p) + g'(x - p)), the linear part bounded by the Neumaier-Shcherbina formula with outward rounding; a node the IPM does not finish is decided by its LP (same feasible set) when infeasible, else re-solved by Condat-Vu. No warm start (the IPM has no warm-start entry yet) | Friedlander & Orban, *A primal-dual regularized interior-point method for convex quadratic programs*, Math. Program. Comput. 4 (2012); Neumaier & Shcherbina, *Safe bounds in linear and mixed-integer linear programming*, Math. Programming 99 (2004); Gupta & Ravindran, *Branch and bound experiments in convex nonlinear integer programming*, Management Science 31(12) (1985) | `src/mip/branch_and_bound_miqp.cpp`, `tests/unit/test_miqp_node_ipm.cpp` |
| Parallel tree search (#222, `mip_threads`): each worker runs the sequential search on a subtree, sharing the incumbent, the node count, the pool and the pseudocosts; an idle worker takes a subtree another gives away | Ralphs, Shinano, Berthold & Koch, *Parallel Solvers for Mixed Integer Linear Optimization*, in Hamadi & Sais (eds.), *Handbook of Parallel Constraint Reasoning*, Springer, 2018 | `src/mip/branch_and_bound_parallel.cpp` |
| Exact rational branch and bound (test oracle) | as above, in exact arithmetic | `tests/oracles/rational_simplex.cpp` |
| Shifted geometric mean benchmark reporting | Mittelmann, plato.asu.edu benchmark methodology | `bench/runners/make_benchmarks_doc.py` |
| Performance-variability control for MILP benchmarks: each instance re-run under seeded row and column permutations of its MPS file, per-instance matched and proved counts over the seeds, shifted geometric mean time (shift 10 s, unproved runs charged the limit); time to first feasible and the primal integral from the solver's incumbent trace (#504) | Lodi and Tramontani, *Performance variability in mixed-integer programming*, INFORMS TutORials in Operations Research (2013); Berthold, *Measuring the impact of primal heuristics*, Operations Research Letters 41 (2013) | `bench/runners/miplib_seeds.py`, `bench/runners/miplib.py`, `src/mip/branch_and_bound_node.cpp` |
| Multi-period refinery LP/MILP case study: staircase mass-balance structure, crude distillation unit and downstream unit capacities, quality budget constraints for product-pool blending, delivery commitments; binary crude-purchase decisions in the MILP variant; synthetic data, KKT-verified LP optimum by construction | Floudas, C.A. and Lin, X., *Mixed Integer Linear Programming in Process Scheduling: Modeling, Algorithms, and Applications*, Annals of Operations Research 139(1), 2005; Pochet, Y. and Wolsey, L.A., *Production Planning by Mixed Integer Programming*, Springer, 2006 | `bench/case_studies/refinery/generator.py`, `bench/case_studies/refinery/run_case_study.py`, `docs/case_studies/refinery.md` |
| LP duality checks (feasibility, complementary slackness, strong duality) | Chvátal, *Linear Programming*, ch. 5 | `tools/verify_solution.py` |
| The `.qplib` file format, read and converted to QPS for the QPLIB benchmark (#492): the section order and omissions by problem type, and the quadratic convention the files use - 0.5 x'Q x with Q the lower triangle as listed (the website's reading), so an off-diagonal is halved into QPS's symmetric QUADOBJ - established by evaluating QPLIB's published points, not by choice | Furini et al., *QPLIB: a library of quadratic programming instances*, Mathematical Programming Computation 11 (2019), Appendix B, Table 8; https://qplib.zib.de/doc.html (the triangular Q^i and its footnote) | `bench/runners/qplib_format.py`, checked per instance by `bench/runners/fetch_qplib.py` |
| QP optimality measures for the Maros-Meszaros benchmark (#491): primal residual, dual residual (distance of the row multipliers and the derived reduced costs from the admissible signs) and duality gap against the Dorn dual, absolute and relative, success at 1e-6 and 1e-9 | Dorn, *Duality in quadratic programming*, Quarterly of Applied Mathematics 18 (1960); Nocedal & Wright, *Numerical Optimization*, 2nd ed., ch. 12 and 16; the measures and levels of the published qpbenchmark Maros-Meszaros report (Caron et al., `qpsolvers/maros_meszaros_qpbenchmark`, `results/maros_meszaros.md`) | `bench/runners/qp_residuals.py`, `bench/runners/maros_meszaros.py` |
| Pooling problem formulations for the standard instances (#516): the P-formulation (pool qualities as columns, bilinear quality balances), the Q-formulation (source proportions, path flows v = q*y as columns with bilinear defining rows) and the PQ-formulation (Q plus the RLT rows sum_i v_ilj = y_lj and sum_j v_ilj <= cap_l q_il) | Haverly, *Studies of the behavior of recursion for the pooling problem*, ACM SIGMAP Bulletin 25 (1978); Ben-Tal, Eiger & Gershovitz, *Global minimization by reducing the duality gap*, Math. Programming 63 (1994); Quesada & Grossmann, *Global optimization of bilinear process networks with multicomponent flows*, Comput. Chem. Eng. 19 (1995); Tawarmalani & Sahinidis, *Convexification and Global Optimization in Continuous and Mixed-Integer Nonlinear Programming*, Kluwer (2002), ch. 9 | `bench/runners/pooling_models.py`, models in `data/pooling/` |
| Spatial branch and bound for products of columns (#514; `--option nonconvex=global`, off by default): best-bound search over boxes, branching on a column of the most violated product at a point mixing the relaxation value and the box midpoint, `optimal` only when the proven bound closes `mip_relative_gap` / `mip_absolute_gap` | Falk & Soland, *An algorithm for separable nonconvex programming problems*, Management Science 15 (1969); Tawarmalani & Sahinidis, *Convexification and Global Optimization in Continuous and Mixed-Integer Nonlinear Programming*, Kluwer (2002); Belotti, Lee, Liberti, Margot & Wachter, *Branching and bounds tightening techniques for non-convex MINLP*, Optimization Methods and Software 24 (2009), sec. 5 | `src/global/spatial_bnb.cpp` |
| McCormick envelopes of x_a x_b over a box (four inequalities; a secant and three tangents for a square), and a lower bound from ANY LP multipliers by weak duality, so an LP solved to tolerance can only weaken the node bound | McCormick, *Computability of global solutions to factorable nonconvex programs: Part I*, Math. Programming 10 (1976); Al-Khayyal & Falk, *Jointly constrained biconvex programming*, Math. Oper. Res. 8 (1983); Neumaier & Shcherbina, *Safe bounds in linear and mixed-integer linear programming*, Math. Programming 99 (2004) | `src/global/relaxation.cpp` |
| Feasibility-based bound tightening over rows with products, and over objective <= incumbent, with outward-relaxed bounds | Belotti, Lee, Liberti, Margot & Wachter (2009), above, sec. 3 | `src/global/fbbt.cpp` |
| Upper bounds for the global search: the alternating LP (fix one side of a bipartite product graph, solve the LP in the rest, alternate; a vertex cover when the graph is not bipartite), started from the relaxation point and from the factor values that make its products exact; every point re-checked against the original rows | Haverly, *Studies of the behavior of recursion for the pooling problem*, ACM SIGMAP Bulletin 25 (1978); Audet, Brimberg, Hansen, Le Digabel & Mladenovic, *Pooling problem: alternate formulations and solution methods*, Management Science 50 (2004) | `src/global/local_search.cpp` |
| QCMATRIX quadratic rows in MPS (full symmetric matrix, no factor 1/2; (i, j) and (j, i) folded into one product) | IBM ILOG CPLEX file-format reference, QCMATRIX section; Gurobi reference manual, MPS format | `src/io/mps_quadratic.cpp`, `tools/verify_solution_mps.py` |
| Lifted knapsack cover cuts at the root, exact sequential lifting through a 0/1 knapsack dynamic programme (#159; `--option enable_root_cuts=true`, off by default by measurement) | Balas, *Facets of the knapsack polytope*, Math. Programming 8 (1975); Wolsey, *Faces for a linear inequality in 0-1 variables*, ibid.; Zemel, *Easily computable facets of the knapsack polytope*, Math. Oper. Res. 14 (1989); Crowder, Johnson & Padberg, Oper. Res. 31 (1983); Gu, Nemhauser & Savelsbergh, INFORMS J. Computing 10 (1998) | `src/mip/cuts.cpp` |
| LP engine selection behind `algorithm=auto` (#284): a rule table on rows, nonzeros and the presence of a starting basis, every threshold tied to a CSV in `bench/results/`; a selected interior point that declines falls back to the dual simplex | no algorithm: a policy; the measurements are in `docs/BENCHMARKS.md` sections 1a and 1f | `src/core/engine_selection.cpp` |
| Mixed-integer rounding (MIR) cuts at the root from single model rows: bound substitution to the nearer bound, divisors 1 and the fractional integer coefficients, the most violated member kept (#221; part of `enable_root_cuts`, `enable_mir_cuts` isolates the family) | Nemhauser & Wolsey, *A recursive procedure to generate all cuts for 0-1 mixed integer programs*, Math. Programming 46 (1990); Marchand & Wolsey, *Aggregation and mixed integer rounding to solve MIPs*, Oper. Res. 49 (2001) | `src/mip/mir_cuts.cpp` |
| c-MIR (#498; `mir_cmir`, off by default): each continuous column of an MIR base row substituted by its closest simple or variable bound (x <= u y + c or x >= l y + c, read off two-nonzero rows), divisors 1 and |a_j| of the integer columns strictly inside their bounds, then delta/2, /4, /8 after the best, complementation of the integer columns nearest their midpoint, the most efficacious cut kept; coefficients under the zero drop paid for over the global box | Marchand & Wolsey, *Aggregation and mixed integer rounding to solve MIPs*, Oper. Res. 49(3) (2001); Nemhauser & Wolsey, *Integer and Combinatorial Optimization* (Wiley, 1988), ch. II.1.7 | `src/mip/mir_cmir.cpp` |
| Flow cover cuts for the fixed-charge structure: variable upper bounds read off two-nonzero rows x <= u y, a cover of the switched inflows chosen at the LP point, each outflow on the right in its smaller form, unlifted; validity checked in exact arithmetic over every switch assignment (#419; `enable_flow_cover_cuts`, off by default) | Padberg, Van Roy & Wolsey, *Valid linear inequalities for fixed charge problems*, Oper. Res. 33 (1985); Gu, Nemhauser & Savelsbergh, *Lifted flow cover inequalities for mixed 0-1 integer programs*, Math. Programming 85 (1999), for the lifting not built | `src/mip/flow_cover_cuts.cpp` |
| Implied-bound cuts: on a row with one continuous and one binary column, the line through the continuous column's bounds at the binary's two values, each capped by the column's own bound; tighter than the row exactly when a cap binds; both sides of a ranged row, case bounds widened by the feasibility tolerance (#499; `mip_implied_bound_cuts`, off by default) | Savelsbergh, *Preprocessing and probing techniques for mixed integer programming problems*, ORSA J. Computing 6 (1994); Achterberg, *Constraint Integer Programming*, PhD thesis, TU Berlin (2007), sec. 8.3 | `src/mip/implied_bound_cuts.cpp` |
| Activity-based bound propagation in synchronous (Jacobi) rounds: every row read against the bounds the round began with, each column taking the tightest implied bound, integer bounds rounded inward, to a fixpoint or a round cap; a CPU reference and a CUDA version (one thread per row, one per column, atomic min/max, explicit round-to-nearest arithmetic so no fused multiply-add) that return identical bounds (#510; `gpu_domain_prop`, off by default, at the MIP root) | Savelsbergh, *Preprocessing and probing techniques for mixed integer programming problems*, ORSA J. Computing 6 (1994); Sofranac, Gleixner & Pokutta, *Accelerated domain propagation for mixed-integer linear programs on GPUs*, arXiv:2009.07785 (2020), Algorithm 1 | `src/mip/domain_propagation.cpp`, `src/gpu/domain_prop.cu` |
| Cut selection: score by efficacy, objective parallelism and integer support, a greedy pick under a per-round cap with an orthogonality filter against the round's chosen set, the rest kept waiting for the next round (#415; `cut_max_per_round`, `cut_max_parallelism`) | Wesselmann & Suhl, *Implementing cutting plane management and selection techniques*, technical report, University of Paderborn (2012); Achterberg, *Constraint Integer Programming*, thesis, TU Berlin (2007), ch. 8 | `src/mip/cut_selection.cpp` |
| Root separation loop (#495; `root_cut_loop`, off by default): after the first root round, separate every enabled family again at the new LP point (Gomory from the new basis, the waiting cuts re-offered), select, append, re-solve warm with the dual simplex, and stop when the last 3 rounds moved the bound by at most 1e-3 of the gap (or of max(1, |bound|) with no incumbent), after 20 rounds, at 20 percent of the time limit, on a round that takes nothing, or once the loop has added max(100, m) rows in all, m the rows before the first cut (the row budget, a round that would pass it taking its best cuts up to it); one log line per round | Achterberg, *Constraint Integer Programming* (thesis, TU Berlin, 2007), ch. 8; Achterberg & Wunderling, *Mixed integer programming: analyzing 12 years of progress*, in Facets of Combinatorial Optimization (Springer, 2013); Wesselmann & Suhl, *Implementing cutting plane management and selection techniques*, Technical report, University of Paderborn (2012) | `src/mip/branch_and_bound_root_loop.cpp` |
| Dual steepest-edge pricing (#411; `pricing=dual-steepest-edge`, not the default until its A/B): the leaving row priced by the exact squared norm of its row of the basis inverse, maintained through the Forrest-Goldfarb update with one extra FTRAN per pivot, the leaving row's norm re-read from the iteration's own BTRAN, exact at the start on warm bases up to 2,000 rows; the maintained weights are held to recomputed norms to a relative 1e-9 in the unit test | Forrest & Goldfarb, *Steepest-edge simplex algorithms for linear programming*, Math. Programming 57 (1992); Koberstein, *The dual simplex method, techniques for a fast and stable implementation*, thesis, Universitaet Paderborn (2005), ch. 5 | `src/simplex/dual_simplex.cpp` |
| Gomory mixed-integer cuts at the root, from tableau rows reconstructed off the final basis (#159) | Gomory, *An algorithm for the mixed integer problem*, RAND RM-2597 (1960); Balas, Ceria, Cornuéjols & Natraj, *Gomory cuts revisited*, Oper. Res. Letters 19 (1996); Marchand & Wolsey, *Aggregation and mixed integer rounding to solve MIPs*, Oper. Res. 49 (2001) | `src/mip/cuts.cpp` |
| GMI safety (#496 item 4; `gmi_safety`, off by default): no Gomory cut from a source row whose basic value is within 0.01 of an integer (every coefficient is divided by f0 or 1 - f0), and every emitted cut's right-hand side loosened by 1e-9 + 1e-9 abs(rhs); the maximum dynamism is the filter's existing coefficient-ratio cap of 1e6, applied to every family | Cornuejols, Margot & Nannicini, *On the safety of Gomory cut generators*, Math. Programming Computation 5 (2013) | `src/mip/cuts.cpp` |
| Bound rounding on integral rows; cut filtering by density, coefficient range and violation, duplicates dropped | Chvátal, *Edmonds polytopes and a hierarchy of combinatorial problems*, Discrete Math. 4 (1973); Achterberg, *Constraint Integer Programming* (thesis, 2007), ch. 8 | `src/mip/cuts.cpp` |
| Cut pool (#497; `mip_cut_pooling`, off by default): a cut row freed by age (`mip_cut_age_limit` consecutive node solves with its slack basic, default 50) keeps its cut, and when a later node's LP point violates it by more than the cut filter's violation tolerance its right-hand side is re-imposed and the node re-solved from its own basis. Rows are freed in place, not deleted, so every stored basis stays valid. Fuzzed in ctest under the debug-solution check (#500) at an age limit of 1 on binary knapsack and covering instances whose unique optimum is found by enumeration, with the option off as the control | Achterberg, *Constraint Integer Programming* (thesis, TU Berlin, 2007), ch. 8 | `src/mip/branch_and_bound_cuts.cpp`, `tests/unit/test_debug_solution.cpp` |
| Cut validity gate: every family checked in exact arithmetic against the rational oracle's optimum, with a deliberately invalid cut as the negative control | as above, in exact arithmetic | `tests/unit/test_cuts.cpp` |
| Debug-solution check (#500; `--option debug_solution=<file>`, off by default): a known feasible or optimal point read by column name, and every presolve reduction, integral row rounding, cut candidate and cut row (root and tree), node propagation, node LP bound and reduced-cost fixing checked against it; the first that removes it aborts naming the family, round, node and row. Fuzzed in ctest on random and binary MILPs whose unique optimum is found by exhaustive enumeration, with every family and reduction on, and a planted invalid cut and a planted invalid presolve reduction as the negative controls | The debug-solution mechanism as solver user manuals describe it, implemented from that description; validity of an inequality as in Nemhauser & Wolsey, *Integer and Combinatorial Optimization* (Wiley, 1988), ch. II.1 | `src/mip/debug_solution.cpp`, `src/mip/branch_and_bound_debug.cpp`, `tests/unit/test_debug_solution.cpp` |
| Certificate of infeasibility (Farkas): row multipliers whose aggregate no point in the column box satisfies, checked against the ORIGINAL model before it is published (#191) | Farkas, J., *Theorie der einfachen Ungleichungen*, J. reine angew. Math. 124 (1902); Schrijver, *Theory of Linear and Integer Programming* (1986), section 7.3 | `src/core/certificate.cpp`, `include/sankhya/certificate.hpp` |
| Irreducible Infeasible Subsystem (IIS): deletion filter starting from the Farkas certificate support (O(k) re-solves where k is the number of named constraints); each candidate is freed and re-solved — redundant if still infeasible, necessary otherwise (#217) | Chinneck, J.W. and Dravnieks, E.W., *Locating minimal infeasible constraint sets in linear programs*, ORSA Journal on Computing 3(2), 1991, pp. 157–168 | `src/core/iis.cpp`, `include/sankhya/model.hpp` |
| Certificate of unboundedness: a recession direction no bound blocks along which the objective strictly improves, reported with the feasible point it starts from (#191) | Schrijver, ibid., section 8.2; Chvatal, *Linear Programming* (1983), ch. 3 on the ratio test that exhibits the ray | `src/core/certificate.cpp`, `src/simplex/primal_simplex.cpp` |
| Convex QP by a primal-dual proximal method with a forward step for the smooth 0.5 x'Qx term (#55; the LP engine's iteration with the gradient added) | Condat, *A primal-dual splitting method for convex optimization involving Lipschitzian, proximable and linear composite terms*, J. Optim. Theory Appl. 158 (2013); Vũ, *A splitting algorithm for dual monotone inclusions involving cocoercive operators*, Adv. Comput. Math. 38 (2013); Chambolle & Pock (2011) for the Q = 0 case | `src/qp/qp_condat_vu.cpp` |
| Convex QP by a proximal (primal-dual regularized) interior point (#490, `qp_algorithm=ipm`, off by default): rows as equalities with slack columns, Mehrotra predictor-corrector with sigma = (mu_aff/mu)^3 and fraction-to-boundary 0.995, proximal centres at the current iterate, the regularized quasi-definite augmented system factored by the signed LDL^T with the AMD ordering (`SparseLdl::factorize_quasidefinite`) | Friedlander & Orban, *A primal-dual regularized interior-point method for convex quadratic programs*, Math. Program. Comput. 4 (2012); Schwan, Jiang, Kuhn & Jones, *PIQP: a proximal interior-point quadratic programming solver*, arXiv 2304.00290 (2023), paper only; Mehrotra, SIAM J. Optim. 2(4) (1992); Vanderbei, *Symmetric quasidefinite matrices*, SIAM J. Optim. 5(1) (1995) | `src/qp/qp_ipm.cpp`, `src/qp/qp_ipm_system.cpp`, `src/la/ldl.cpp` |
| Convexity decided before any arithmetic: LDL^T without interchanges on sense · Q, a negative pivot returned as the certificate; dense, and above 2000 columns it reports unverified rather than guessing | Golub & Van Loan, *Matrix Computations* (4th ed.), section 4.1; Higham, *Accuracy and Stability of Numerical Algorithms* (2nd ed.), ch. 10 | `src/qp/convexity.cpp` |
| Presolve: empty, singleton, redundant and forcing rows, fixed and empty columns (#43), free column singletons and doubleton equations (#92), dual fixing (#412; `presolve_dual_fixing`, off by default: a column whose entries can only push their rows away from a finite bound one way, and whose cost never rewards that way, is fixed at the other bound and replayed nonbasic there), parallel rows (#412; `presolve_parallel_rows`, off by default: a row that is a scalar multiple of an earlier one folds its scaled bounds into that row and goes, its dual handed back to it when the bound that binds was its own), implied-integer detection (#513; `presolve_implied_integer`, off by default until an A/B on main; exact integer data only, and only in a model that already has integer columns: a continuous variable in an equality row where every other live variable is already integer with an integer coefficient and the remaining coefficient divides both the RHS and all other coefficients is promoted to integer and treated as such by the engine; runs to a fixpoint); every reduction pushes a record and postsolve replays the stack in reverse, with the round-trip asserted against the rational oracle | Brearley, Mitra & Williams, *Analysis of mathematical programming problems prior to applying the simplex algorithm*, Math. Programming 8 (1975); Andersen & Andersen, *Presolving in linear programming*, Math. Programming 71 (1995); Achterberg et al., *Presolve reductions in mixed integer programming*, INFORMS J. Computing 32 (2020) | `src/presolve/presolve.cpp` |
| Presolve, bound propagation from row activity (#485; `presolve_bound_propagation`, off by default): on each live inequality row with two or more columns, each column's bounds implied by the row's bounds and the activity range of the other columns, loosened by a bound on its own rounding error ((terms + 3) eps of the row's magnitude, Higham's gamma_n; none for exact integer data), rounded inward on an integer column, written only on a `kPresolveBoundMinStep` improvement; a column singleton in an inequality row and a doubleton inequality are special cases; rows that are combinations of original rows (fill-in, parallel merge) and doubleton-fold columns are left alone. Each tightened bound is a `kImpliedBound` record: when the engine ends a column on it, postsolve moves the reduced cost onto the implying row (y_i += d_j / a_ij, in the dual passes' fixed point, a bound propagated from a propagated bound undone first) and restores a basis of the original model around the column | Brearley, Mitra & Williams, *Analysis of mathematical programming problems prior to applying the simplex algorithm*, Math. Programming 8 (1975); Andersen & Andersen, *Presolving in linear programming*, Math. Programming 71 (1995); Higham, *Accuracy and Stability of Numerical Algorithms* (2nd ed.), sec. 3.1 (gamma_n); Cederberg and Boyd, *Presolve for first-order LP methods*, arXiv 2604.23951 (paper only, judgement call 13) | `src/presolve/presolve.cpp`, `src/presolve/implied_bounds.cpp` |
| Presolve, coefficient tightening and big-M strengthening on integer rows (#511; `presolve_coefficient_tightening`, off by default): on a row whose maximum activity M over the box exceeds its bound b by g = M - b, an integer column's coefficient larger than g is reduced to g and b moved to match, so x <= M y with x <= U < M becomes x <= U y; alternates with activity-based bound propagation whose bounds are written into the reduced model; no postsolve record, the integer points are unchanged, checked by enumerating every integer point of random models before and after and against the exact MILP oracle | Savelsbergh, *Preprocessing and probing techniques for mixed integer programming problems*, ORSA J. Computing 6(4) (1994); Achterberg, Bixby, Gu, Rothberg & Weninger, *Presolve reductions in mixed integer programming*, INFORMS J. Computing 32(2) (2020), sec. 3 | `src/presolve/coef_tightening.cpp` |
| Binary probing with a clique table (#512; `presolve_probing`, off by default): each binary, most constrained first within a work limit, fixed to 0 and to 1 with the rows propagated; one value infeasible fixes the other, a bound both values agree on is tightened, a binary one value fixes is a conflict between two literals; conflicts merged greedily into maximal cliques; the fixings and tightenings become the reduced model's bounds, and in the search the literal conflicts (complemented literals included) join the graph the clique separator reads, a clique over literals giving sum x_j + sum (1 - x_k) <= 1; every deduction checked against every integer point of random models | Savelsbergh, *Preprocessing and probing techniques for mixed integer programming problems*, ORSA J. Computing 6(4) (1994); Atamturk, Nemhauser & Savelsbergh, *Conflict graphs in solving integer programming problems*, European J. Operational Research 121 (2000); Achterberg, Bixby, Gu, Rothberg & Weninger, *Presolve reductions in mixed integer programming*, INFORMS J. Computing 32(2) (2020) | `src/presolve/probing.cpp`, `src/mip/combinatorial_cuts.cpp` |
| Root diving heuristic: fix the least-fractional integer column, re-solve, to a bounded depth, so the search starts with an incumbent (#25) | Achterberg, *Constraint Integer Programming* (thesis, 2007), ch. 6; Berthold, *Primal heuristics for mixed integer programs* (diploma thesis, TU Berlin, 2006) | `src/mip/branch_and_bound.cpp` |
| Singular-basis repair: the LU reports dependent columns by position, the simplex evicts them for their logicals, and a budget of stalled repairs stops a solve that is being carried by repair alone (#34, #174) | Maros, *Computational Techniques of the Simplex Method*, section 9.4; Suhl & Suhl, *Computing sparse LU factorizations for large-scale linear programming bases*, ORSA J. Computing 2 (1990) | `src/la/lu.cpp`, `src/simplex/simplex_core.hpp` |
| Nonlinear expression DAG: hash-consed construction, safe simplification, constant folding (#296) | Standard common-subexpression elimination by value numbering; written from the definition | `src/nlp/expression.cpp` |
| Exact gradients (reverse-mode AD) and Hessians (forward-over-reverse, second-order adjoints) (#296) | Griewank & Walther, *Evaluating Derivatives*, 2nd ed. (SIAM, 2008), ch. 3 and 5 | `src/nlp/derivatives.cpp` |
| Convexity by composition rules over interval ranges (#296) | Grant, Boyd & Ye, "Disciplined convex programming", in *Global Optimization* (Springer, 2006); Boyd & Vandenberghe, *Convex Optimization* (2004), sec. 3.2.4 | `src/nlp/convexity.cpp` |
| Structural sparsity of an expression's Hessian by index-domain propagation of nonlinear interactions (NLP stage 1): a product adds domain(a) x domain(b), a quotient also domain(b)^2, a power or a unary function domain(a)^2, a sum or negation nothing; a superset of every numerical pattern, so the factorization's symbolic analysis runs once | Walther, "Computing sparse Hessians with automatic differentiation", *ACM Transactions on Mathematical Software* 34(1) (2008); Griewank & Walther, *Evaluating Derivatives*, 2nd ed. (SIAM, 2008), ch. 7 | `src/nlp/sparsity.cpp` |
| Compiled expression tapes: each expression renumbered onto its own tape, gradient by one reverse sweep, Hessian by forward-over-reverse swept only over the columns of its structural pattern; the per-node value and derivative rules shared with the graph evaluator in one header, sin and cos added (NLP stage 1) | Griewank & Walther, *Evaluating Derivatives*, 2nd ed. (SIAM, 2008), ch. 2, 3 and 5 | `src/nlp/tape.cpp`, `src/nlp/derivative_rules.hpp` |
| A nonlinear model as the smooth NLP min f(x) s.t. g_L <= g(x) <= g_U, x_L <= x <= x_U, with the Jacobian on a fixed row pattern and the Hessian of the Lagrangian sigma d2f + sum lambda_i d2g_i on a fixed lower-triangle pattern (NLP stage 1) | The problem form of Wachter & Biegler, "On the implementation of an interior-point filter line-search algorithm for large-scale nonlinear programming", *Mathematical Programming* 106(1) (2006) | `src/nlp/nlp_problem.cpp` |
| Reader for AMPL's text `.nl` format: header, the C/O/V/J/G/r/b/x/d/k/S segments, expression graphs in prefix notation read iteratively, integer columns from the variable-ordering tables; non-smooth and logical operators, imported functions, complementarity and the binary form refused by name (NLP stage 1) | Gay, "Writing .nl Files", Sandia report SAND2005-7907P (2005), secs. 2-4 and Tables 4, 6, 8, 11, 13, 17; Gay, "Hooking Your Solver to AMPL" (AT&T Bell Laboratories, 1993, rev. 1997), Tables 3 and 4 | `src/nlp/nl_reader.cpp` |
| Optimality-based bound tightening (OBBT) at the root (#515): for each non-fixed variable solve min/max x_j over the LP relaxation augmented with c'x <= incumbent - epsilon, writing tighter bounds back before the search begins | Gleixner, Berthold, Muller and Weltge, *Three enhancements for optimization-based bound tightening*, J. Global Optimization 67 (2017), sec. 2 | `src/mip/obbt.cpp` |
| Strong-branch fixing: a probe that proves one side of a column infeasible fixes the column to the other side for the whole subtree (the fixes hang under the children as never-opened link nodes), the node LP is re-solved and the decision retaken, for a bounded number of rounds; both sides infeasible fathoms the node (`mip_strong_branch_fix`, off by default, #502) | Achterberg, Koch & Martin, *Branching rules revisited*, Operations Research Letters 33 (2005), 42–54; Achterberg, *Constraint Integer Programming* (thesis, TU Berlin, 2007), ch. 5 | `src/mip/branch_and_bound_fixpoint.cpp`, `src/mip/branch_and_bound_node.cpp` |
| Incremental node propagation to a fixpoint: a worklist of the rows whose columns' bounds moved, under a work budget, in place of three full sweeps (`mip_incremental_propagation`, off by default, #502) | Achterberg, *Constraint Integer Programming* (thesis, TU Berlin, 2007), ch. 7; Savelsbergh, *Preprocessing and probing techniques for mixed integer programming problems*, ORSA J. Computing 6(4), 1994 | `src/mip/branch_and_bound_fixpoint.cpp` |
| Binary-heap open list: O(log n) next-node selection for best-bound and best-estimate, with the linear scan's key and tie-break so the search is unchanged (`mip_heap_open_list`, off by default, #502) | Achterberg, *Constraint Integer Programming* (thesis, TU Berlin, 2007), ch. 6; Cormen, Leiserson, Rivest & Stein, *Introduction to Algorithms*, 3rd ed. (2009), ch. 6 | `src/mip/branch_and_bound_fixpoint.cpp`, `src/mip/branch_and_bound_node.cpp` |

Of the work once listed here as "Phases 6 onwards", restarted PDHG, the Mehrotra
predictor-corrector, Gomory mixed-integer and lifted cover cuts, Devex pricing (#66, the
default) and the Harris ratio test (#67, opt-in) have all landed and are in the table
above. Not landed: the Forrest-Tomlin update (Forrest & Tomlin 1972; what ships is the
product form of the inverse with refactorization forced by an accuracy check),
mixed-integer rounding cuts, and cuts below the root.

---

## 4. Machine-checkable evidence

### 4.1 What the binary actually links against

Regenerated at **Phase 2**, commit `12fa98b`. The Phase 1 text predicted that zlib would
appear here once the gzip MPS path became real; it now does, on both platforms, which is
what that prediction was for.

Windows 11, **GCC 16.1.0 (MSYS2 UCRT64), Release**:

```
$ objdump -p build/sankhya.exe | grep "DLL Name" | sort -u
        DLL Name: KERNEL32.dll
        DLL Name: api-ms-win-crt-convert-l1-1-0.dll
        DLL Name: api-ms-win-crt-environment-l1-1-0.dll
        DLL Name: api-ms-win-crt-filesystem-l1-1-0.dll
        DLL Name: api-ms-win-crt-heap-l1-1-0.dll
        DLL Name: api-ms-win-crt-locale-l1-1-0.dll
        DLL Name: api-ms-win-crt-math-l1-1-0.dll
        DLL Name: api-ms-win-crt-private-l1-1-0.dll
        DLL Name: api-ms-win-crt-runtime-l1-1-0.dll
        DLL Name: api-ms-win-crt-stdio-l1-1-0.dll
        DLL Name: api-ms-win-crt-string-l1-1-0.dll
        DLL Name: api-ms-win-crt-time-l1-1-0.dll
        DLL Name: api-ms-win-crt-utility-l1-1-0.dll
        DLL Name: zlib1.dll
```

The Windows C runtime plus **zlib1.dll**. Note that the `-static` flag added in Phase 1 does
not cover this one: MSYS2 provides zlib as an import library (`libz.dll.a`), so the Windows
`.exe` is no longer self-contained. That is recorded rather than fixed. Windows is the
convenience build; Linux is what CI gates and what the claim rests on. It is logged as
judgement call 7 below.

The Linux build is the authoritative one. Produced by the `provenance` job on
**ubuntu-latest, GCC (runner default), Release**, run 32867892122:

```
$ ldd build/sankhya
        linux-vdso.so.1 (0x00007f2012bf2000)
        libz.so.1 => /lib/x86_64-linux-gnu/libz.so.1 (0x00007f2012b07000)
        libstdc++.so.6 => /lib/x86_64-linux-gnu/libstdc++.so.6 (0x00007f2012800000)
        libm.so.6 => /lib/x86_64-linux-gnu/libm.so.6 (0x00007f2012717000)
        libgcc_s.so.1 => /lib/x86_64-linux-gnu/libgcc_s.so.1 (0x00007f2012ad9000)
        libc.so.6 => /lib/x86_64-linux-gnu/libc.so.6 (0x00007f2012400000)
        /lib64/ld-linux-x86-64.so.2 (0x00007f2012bf4000)

$ ldd build/sankhya | grep -Ei 'cbc|clp|highs|scip|soplex|glpk|lpsolve|osqp|ortools|gurobi|cplex|xpress'
OK: no solver library is linked
```

zlib, the C++ runtime, libc and libm - and, on a build with `SANKHYA_WITH_OPENMP` on (the
default, #57), the compiler's own OpenMP runtime, `libgomp` with GCC, which the listing
above predates. Nothing else.

That grep is a **gate**, not a comment: the `provenance` job in `.github/workflows/ci.yml`
exits non-zero if any linked library name matches a known solver. The claim cannot silently
rot.

### 4.2 The full link line

Asked of Ninja directly so it cannot drift from what was really executed.

Linux, from the CI `provenance` job (run 32867892122):

```
$ ninja -C build -t commands sankhya | tail -1
/usr/bin/c++ -O3 -DNDEBUG -Wl,--dependency-file=CMakeFiles/sankhya-cli.dir/link.d     CMakeFiles/sankhya-cli.dir/apps/sankhya-cli/main.cpp.o     -o sankhya     libsankhya_core.a     _deps/fmt-build/libfmt.a     /usr/lib/x86_64-linux-gnu/libz.so
```

Windows, same command on the local tree:

```
$ ninja -C build -t commands sankhya-cli | tail -1
g++ -O3 -DNDEBUG -static-libgcc -static-libstdc++ -static     CMakeFiles/sankhya-cli.dir/apps/sankhya-cli/main.cpp.obj     -o sankhya.exe     -Wl,--out-implib,libsankhya.dll.a     libsankhya_core.a     _deps/fmt-build/libfmt.a     C:/msys64/ucrt64/lib/libz.dll.a
```

Three artifacts on each platform: our own core, fmt, and the system zlib. CLI11 and
nlohmann/json are header-only and appear as includes rather than archives; they are in the
dependency table above and visible in `CMakeLists.txt`.

### 4.2b The opt-in cuDSS build (#489)

Not the default and not what CI gates: a build configured with `-DSANKHYA_ENABLE_CUDA=ON
-DSANKHYA_CUDA_ARCHITECTURES=70 -DSANKHYA_ENABLE_CUDSS=ON -DCUDSS_ROOT=<site-packages>/nvidia/cu12`
at commit `40255e2`, on an NVIDIA V100 box (Ubuntu 22.04, GCC 11.4.0, CMake 3.22.1, CUDA 12.4,
driver 550.127.08), cuDSS from NVIDIA's `nvidia-cudss-cu12` 0.8.0.10 wheel. The `libcudss.so`
the link line names is a symlink made beside the wheel's `libcudss.so.0`; without it CMake finds
`libcudss.so.0` itself.

```
$ ninja -C build-cudss -t commands sankhya | tail -1
/usr/bin/c++ -O3 -DNDEBUG  CMakeFiles/sankhya-cli.dir/apps/sankhya-cli/main.cpp.o -o sankhya -L/usr/local/cuda/targets/x86_64-linux/lib -Wl,-rpath,/usr/local/cuda/lib64:/usr/local/lib/python3.10/dist-packages/nvidia/cu12/lib  libsankhya_core.a  _deps/fmt-build/libfmt.a  /usr/local/cuda/lib64/libcusparse.so  /usr/local/cuda/lib64/libcudart.so  /usr/local/lib/python3.10/dist-packages/nvidia/cu12/lib/libcudss.so  /usr/lib/x86_64-linux-gnu/libz.so  /usr/lib/gcc/x86_64-linux-gnu/11/libgomp.so  /usr/lib/x86_64-linux-gnu/libpthread.a  -lcudadevrt  -lcudart_static  -lrt  -lpthread  -ldl

$ ldd build-cudss/sankhya
        linux-vdso.so.1
        libcusparse.so.12 => /usr/local/cuda/lib64/libcusparse.so.12
        libcudart.so.12 => /usr/local/cuda/lib64/libcudart.so.12
        libcudss.so.0 => /usr/local/lib/python3.10/dist-packages/nvidia/cu12/lib/libcudss.so.0
        libz.so.1 => /lib/x86_64-linux-gnu/libz.so.1
        libgomp.so.1 => /lib/x86_64-linux-gnu/libgomp.so.1
        libstdc++.so.6 => /lib/x86_64-linux-gnu/libstdc++.so.6
        libm.so.6 => /lib/x86_64-linux-gnu/libm.so.6
        libgcc_s.so.1 => /lib/x86_64-linux-gnu/libgcc_s.so.1
        libc.so.6 => /lib/x86_64-linux-gnu/libc.so.6
        libnvJitLink.so.12 => /usr/local/cuda/lib64/libnvJitLink.so.12
        libpthread.so.0 => /lib/x86_64-linux-gnu/libpthread.so.0
        librt.so.1 => /lib/x86_64-linux-gnu/librt.so.1
        libdl.so.2 => /lib/x86_64-linux-gnu/libdl.so.2
        /lib64/ld-linux-x86-64.so.2
        libcublas.so.12 => /usr/local/cuda/targets/x86_64-linux/lib/libcublas.so.12
        libcublasLt.so.12 => /usr/local/cuda/targets/x86_64-linux/lib/libcublasLt.so.12
```

(load addresses dropped). Against the CUDA build, three more libraries: `libcudss.so.0` itself,
and `libcublas.so.12` and `libcublasLt.so.12`, which cuDSS loads for its dense kernels (the
cuBLAS row of section 2 still holds for our own code: nothing in `src/` calls cuBLAS). None
matches the solver-name grep of 4.1.

### 4.3 SBOM

An SPDX SBOM is generated in CI by the `provenance` job and attached as a build artifact
(`sankhya-sbom.spdx.json`).

---

## 5. Judgement calls

Decisions where the dependency policy needed interpretation. Per `ENGINEERING_RULES.md`, these are
recorded rather than silently made.

| # | Question | Decision | Reasoning |
|---|---|---|---|
| 1 | Does imitating a familiar solver *log layout* (iteration / objective / primal inf / dual inf / time) cross the line? | Allowed | Interface familiarity, not derivation. The layout is visible in published user manuals and screenshots; no source was consulted. It makes the output readable to the industrial audience that reads such logs daily. Same reasoning as the API-shape allowance in `ENGINEERING_RULES.md`. |
| 2 | Does a string-keyed option table with typed values imitate a solver's API? | Allowed | Explicitly permitted by `ENGINEERING_RULES.md` item 5 (API shape). The shape is create/set/solve/query with string options; the registry, parser and storage are our own. |
| 3 | System zlib is picked up in preference to a fetched copy. Does that weaken the provenance claim? | Acceptable, and recorded | zlib is a compression library under a permissive licence, present on essentially every system. The exact resolved path appears in the link line above, so which copy was used is always visible. |
| 4 | The Windows build links libstdc++ and libgcc **statically** (`-static`). | Deliberate | It removes a start-up failure caused by an older MinGW earlier on PATH, and it makes the dependency list above shorter and easier to audit, not longer. It has no effect on the Linux build, which CI treats as authoritative. |
| 5 | The MPS reader imitates the *file format* of CPLEX/Xpress inputs, and the LP reader imitates the CPLEX LP format. | Allowed | A file format is an interface, not an implementation. `ENGINEERING_RULES.md` item 5 covers API shape for the same reason, and reading a format everyone's solver reads is what makes us drop-in adoptable. Both readers were written from the published format specification and from the textbook reference above; **no solver's reader source was consulted**, which `ENGINEERING_RULES.md` calls out by name as forbidden. |
| 6 | Netlib distributes its LP test set in a packed `emps` encoding, not as plain MPS, and expanding it requires the `emps` decoder Netlib ships alongside the data. Does fetching that decoder cross the red line? | **RESOLVED — allowed, and not vendored.** See row 8, which records how it is actually done. | Logged as OPEN while Phase 2 was in progress, and Phase 2 correctly claimed no Netlib result at the time. It was settled when `bench/runners/fetch_data.py` was written: the decoder is DOWNLOADED AND COMPILED at fetch time rather than committed, so this repository contains no third-party source, and the sha256 of everything downloaded is recorded. `emps` is a file-format converter, not a solver, so it sits outside the red line, and `ENGINEERING_RULES.md` item 6 permits the Netlib dataset of which this is the delivery mechanism. Nothing it produces is linked into SANKHYA. The Netlib results in `docs/BENCHMARKS.md` rest on this decision. See issue #62. |
| 7 | The Windows `.exe` dynamically links `zlib1.dll`, so the Phase 1 claim that it is self-contained no longer holds. | Recorded, not fixed | MSYS2 ships zlib only as an import library, and `-static` cannot statically link what has no static archive. Fixing it would mean forcing the bundled zlib build on Windows, which trades an audit-surface improvement for a divergence between the two platforms' dependency sets. Linux is what CI gates and what the provenance claim rests on; Windows is the convenience build. Revisit at Phase 10 packaging if we ship a Windows binary. |
| 8 | `bench/runners/fetch_data.py` downloads Netlib's `emps.c` decoder and COMPILES it at fetch time. Is that third-party source in the project? | Allowed, and not vendored | Netlib distributes its LP set in a custom compressed encoding, and `emps.c` is the decoder they publish beside it. It is a file-format converter, not a solver, so it is outside the red line. It is downloaded at fetch time rather than committed, so this repository contains no third-party source; its sha256 is recorded in `data/netlib/reference.json`. Nothing it produces is linked into SANKHYA - it runs once, offline, to turn an archive format into plain MPS. |
| 9 | MPS says a negative `UP` bound with no explicit lower bound implies `lower = -inf`. That is documented for continuous columns and **implementation-defined for integer ones**, where established readers disagree. Which reading do we take? | Apply the convention to integer columns too, and warn | Declining to choose was tried first and was worse than either choice: leaving `lower = 0` produces `[0, -5]`, an empty interval, so `Model::validate()` rejected the model and the file could not be loaded **at all** - and the resulting error named crossed bounds, which is the symptom rather than the cause. It also put the C++ reader at odds with `tools/verify_solution.py`, which already applies the convention; two components disagreeing about what the same bytes mean is exactly what that verifier exists to catch, so the disagreement sitting inside the pair weakened the check against every instance carrying such a bound. The warning is kept so the ambiguity stays visible in the log. See issue #8. |
| 10 | The device PDHG (#17) cites the cuPDLP paper for its host/device split, and cuPDLP.jl / cuPDLP-C / cuPDLPx are named on the red line. | Allowed, and recorded | The red line forbids reading solver SOURCE. The arXiv paper is a publication, allowed under item 2 of the policy, and it is the only cuPDLP artefact consulted: no repository of any of the three was opened, and the kernels, the CSR upload and the reduction scheme were written from the paper's description and the CUDA / cuSPARSE / CUB documentation. The iteration itself is the CPU engine's, from CP11 and PDLP. |
| 11 | Auditing the 2026 SIH PS26119 field meant READING ten competitors' public repositories at source level, and nine of the ten turn out to contain genuine from-scratch optimization solvers - their simplex implementations, cut generators and PDHG kernels. The red line forbids reading solver source. Does reading a *competitor's* solver cross it? | **Recorded as a judgement call, not silently decided.** The reading was done; nothing from any of those repositories may enter `src/`, and nothing has. | The red line in `ENGINEERING_RULES.md` enumerates established solvers - CBC, Clp, HiGHS, SCIP, SoPlex, GLPK, lp_solve, OSQP, PDLP, cuPDLP, OR-Tools, Gurobi, CPLEX, Xpress - and its purpose is to keep our implementation free of derivation from mature codebases we might otherwise lean on. Ten hackathon repositories a few weeks old are not on that list and are not a plausible source of technique for us; the audit's output is a competitive assessment - threat ranking, evidence discipline, capability gaps - not an algorithm. Every reader agent was instructed to detect any VENDORED solver tree and report its path WITHOUT opening it, precisely so the audit could not launder red-line source through a competitor's repository; no vendored tree was found in any of the ten. The contrary reading is that a from-scratch competitor solver is still a solver, and that the rule is about the act of reading rather than the maturity of what is read. That reading is defensible, which is why this is logged rather than assumed. Two consequences are accepted regardless of how it is resolved: no competitor code is referenced, adapted or cited in `src/`, and if the audit is ever cited externally, this row is the disclosure. |
| 12 | The dual simplex cost perturbation planned in #465 is taken from Koberstein's thesis (2005), whose text remarks that it is similar to the perturbation in an open solver named on the red line. Does learning it from the thesis cross the line? | Allowed, and recorded before implementation | The thesis is a publication, allowed under item 2 of the policy, and the perturbation is specified in it completely (sizes, signs, when to apply and remove it). The resemblance is the author's remark about someone else's code; nobody on this project reads that code, and the implementation is written from the thesis text and Maros (2003) alone. |
| 13 | The first-order presolve planned in #485 follows a 2026 arXiv paper whose authors also publish a presolve library, and that library ships inside GPU LP solvers named on the red line. | Paper only, and recorded before implementation | The paper describes each reduction in enough detail to implement it, and presolve reductions are textbook material (Andersen and Andersen 1995). The library is treated as red-line source because it is distributed inside red-line solvers: it is not opened, and our reductions are written from the paper and from the presolve already in `src/presolve/`. |
| 14 | METIS (#471) would be the first graph-ordering library linked into `src/`. | Allowed, optional, recorded before it is added | METIS partitions graphs; it does not solve an optimization problem, and the policy names graph orderings (AMD, COLAMD, RCM) as allowed when permissively licensed. METIS 5 is Apache-2.0. It is added behind a build option, the CPU build must keep working without it, and the dependency table, SBOM and `ldd` output are updated in the same PR. |
| 15 | cuDSS (#489) is a sparse direct factorization library shipped by NVIDIA, which also ships an optimization solver. | Allowed as a vendor numerical library; **licence checked, see row 28** | The policy allows vendor numerical libraries such as cuSPARSE, cuBLAS and cuSOLVER; cuDSS is the same kind of library and solves linear systems, not optimization problems. The licence check this row waited for is recorded in row 28. |
| 16 | Agreement runs against size-limited commercial editions (#533). | **Allowed as external processes only — licence terms checked 2026-09-24; Gurobi requires an academic licence.** | The same arrangement as the HiGHS comparison: each solver is invoked as a separate process on the same MPS files; nothing is linked, vendored, or read. Licence terms checked and recorded: **(a) Gurobi** — the standard EULA forbids publishing benchmark or comparison results; the exception is an **academic** licence, which does permit it. The size-limited free trial is NOT an academic licence and does not carry the benchmarking exception. `bench/runners/commercial_agreement.py` skips Gurobi unless the runner is instructed to confirm an academic licence is in use (`--gurobi-academic`); no Gurobi CSV is committed without that flag. Size limit ≤2000 variables + constraints enforced in the runner. **(b) CPLEX Community Edition (IBM)** — free for academic non-commercial use; the Community Edition EULA permits benchmarking results to be published for academic or research purposes; size limit ≤1000 variables + constraints enforced in the runner. **(c) GLPK** — GPL-3.0, no size limit, benchmarking always permitted. The runner invokes each as an external subprocess (`gurobi_cl`, `cplex`, `glpsol`), and the comparison output is committed as a CSV naming the instance sha256, our build commit, and the comparison solver version. Nothing from any of these solvers may enter `src/`. |
| 17 | The local-search MIP heuristic planned in #507 follows a CP 2024 paper whose authors publish their implementation, and that implementation is itself a MIP solver. | Paper only, recorded before implementation | The red line exists to keep solver code out of our derivation, and a published local-search MIP solver is a solver whether or not it is on the enumerated list. The paper specifies the moves, weights and tabu rule; the implementation is written from it, and the repository is not opened. The same applies to the Feasibility Jump paper (#506). |
| 18 | The MILP proof certificates planned in #518 use the VIPR certificate format from Cheung, Gleixner and Steffy (2017). A reference checker for that format exists. | Format allowed; our own checker | A certificate format is an interface, like the MPS format (row 5). The checker in `tools/` is written by us in exact rational arithmetic from the paper's definition of the format and never links our C++, the same arrangement as `tools/verify_solution.py`. The reference checker is not read, so that a bug in our reading of the format cannot be masked by copying theirs. |
| 19 | The Maros-Meszaros benchmark (#491) takes its reference objectives from the set's own readme, whose OPT column is the output of BPMPD, an interior-point QP solver, and follows the success criteria of qpbenchmark, a repository that benchmarks QP solvers through a Python wrapper. Does either cross the red line? | Allowed, and recorded | The OPT values are published benchmark data, item 6 of the policy, in the same position as Netlib's readme values for the LP set: they are parsed from `00README.QP` by `bench/runners/fetch_maros_meszaros.py`, never typed, and every one carries its source in `data/maros-meszaros/reference.json`. No BPMPD code exists in this repository or was consulted. Of qpbenchmark only the published results page (`results/maros_meszaros.md`: the three measures, the 1e-6 and 1e-9 levels, the 1000 s limit and the shift of 10) was read; none of its code, the wrapper's code or any solver it calls was opened, and its converted `.mat` copies of the set are not used - the instances come from Maros's own archives, with their sha256 recorded. |
| 20 | PuLP (#536) is pip-installed as an optional modelling library for the adapter, and the wheel ships a CBC binary (a solver named on the red line) alongside it. Does installing it cross the line? | Allowed; the binary is never invoked, and recorded here | The policy forbids copying, vendoring, linking or reading solver source; a binary that pip drops next to a modelling library and that nothing in this repository executes is none of those. The adapter implements PuLP's own `LpSolver` interface and hands the model to `sankhya.Model`; PuLP's default solver is never selected, `test_adapters.py` solves through SANKHYA only, and no benchmark or result in this repository comes from CBC. If a PuLP call ever routed to CBC it would be a bug, not a judgement call. PuLP 4.0.0 (#690) bundles no solver binary, but its `pulp/__init__.py` loads highspy's compiled `_core` library into the process with `ctypes.CDLL(..., RTLD_GLOBAL)` whenever highspy is installed - and in CI it is, as a dependency of CVXPY. That is PuLP loading a HiGHS binary, not this repository linking, calling or reading one: the adapter never selects a PuLP solver and nothing calls into the loaded library. Recorded here for the same reason as the CBC binary. |
| 21 | `bench/runners/gpu_pdlp_compare.py` runs OR-Tools PDLP (a named red-line solver) as a comparison target, installed from the published `ortools` PyPI wheel. Does using the wheel cross the red line? | **Allowed — same pattern as highspy, logged here.** | OR-Tools is named on the red line because its source, if consulted, could inform our implementation. The benchmark runs it as a separate Python process through its published API, precisely as `bench/runners/cross_check_highs.py` runs HiGHS: no source repository is cloned, no header or object file is linked into SANKHYA, and `src/` remains unaware that OR-Tools exists. The published wheel is a binary artefact, not source. The comparison output is committed as a CSV naming the instance, our build commit and the ortools version; it measures competitive positioning, not an algorithm. Nothing from OR-Tools may enter `src/`. This row is the disclosure. |
| 22 | The restarted Halpern iteration (the paper's reflection step is not implemented) for LP PDHG (#481) follows Lu & Yang, "Restarted Halpern PDHG for linear programming", arXiv:2407.16144, 2024, and uses restart constants and safeguards from Chen, Sun, Yuan, Zhang & Zhao, "HPR-LP", arXiv:2408.12179, 2024. The original Halpern fixed-point result is Halpern, "Fixed points of nonexpanding maps", Bull. AMS 73, 1967. Are any of the reference implementations (cuPDLP.jl, HPR-LP) on the red line? | **Papers only — no reference code read. Recorded before implementation.** | cuPDLP.jl is a Julia LP solver and HPR-LP is an LP solver; both are on the red line by class. Only the three papers above were read. The iteration formula, restart rule (fixed-point residual ||z - T(z)||_P), and safeguards are derived from the papers. No code from cuPDLP.jl, HPR-LP or any other LP solver was opened. Implementation is in `src/pdhg/pdhg_halpern.cpp`, behind `pdhg_halpern=false` (default OFF) until an A/B benchmark on `main` confirms the speedup. |
| 23 | The two-mat-vec optimisation (#479) caches A*x_k between iterations to eliminate a redundant sparse product. The idea is standard in first-order methods; it is also described in the context of the Halpern form in Lu & Yang, arXiv:2407.16144 (row 22). No separate reference implementation exists. | **No red-line contact. Recorded for completeness.** | The optimisation is a straightforward consequence of the iteration algebra: A*x_{k+1} is computed once, then A*x_bar and A*dx are derived by vector operations. No solver source was read. Implementation is behind `pdhg_two_matvec=false` (default OFF) until an A/B on CPU and GPU confirms per-iteration time savings. |
| 24 | The standard pooling instances (#516) take their network data from Alfaki and Haugland's published GAMS data files (the directory also holds their GAMS formulation files, and its readme names a commercial global solver as the default), and their reference optima from MINLPLib's instance pages, which list the dual bounds of several global solvers named on the red line or commercial. Does either cross the line? | Allowed, and recorded | The data files hold sets, tables and parameters only (arc costs, adjacency, qualities, capacities): instance data, item 6 of the policy. They are parsed by `bench/runners/fetch_pooling.py`, never typed, with each file's sha256 in `data/pooling/instances/`; the formulation files (`pqmodel.gms`, `tpmodel.gms`, `stpmodel.gms`) were not downloaded or read, and the P-, Q- and PQ-models are written from the papers cited in the algorithm table. From MINLPLib only the published numbers were read (primal bound, the dual bounds that prove it optimal, and the instance size counts used as a structural cross-check in `bench/runners/test_pooling.py`); the flattened instance listing an instance page shows was seen, but no model file or solver code from MINLPLib is used. Licence: the data files carry none; they are numerical problem data from published papers, redistributed here with attribution as benchmark data, on the same basis MINLPLib republishes them. |
| 25 | The proximal interior point for convex QP (#490, #614) takes one idea from the PIQP paper (Schwan, Jiang, Kuhn & Jones, arXiv 2304.00290, 2023): the proximal centres move to the current iterate, so the regularization changes the matrix and never the residual. PIQP is also an open-source solver, which is why this is logged: the paper was read, the solver's code was not. The constants around it are this project's own choices, named in `tolerances.hpp` (`kQpIpmPivotShare`, `kQpIpmRegularizationRaise` x100, `kQpIpmRegularizationAttempts` 8, `kQpIpmComplementarityShare`), picked on the Maros-Meszaros smallest 40 and not taken from PIQP. | Allowed: a paper, not source. |
| 26 | The MILP certificate (#518) uses the VIPR file format, whose authors also publish a reference checker and an exact MIP solver that writes it. | Format from the paper only | The format is an interface described in Cheung, Gleixner and Steffy (IPCO 2017); the writer and `tools/verify_certificate.py` were written from that description. The reference checker's source and the source of every exact MIP solver were deliberately not opened. Where the paper left a detail open (the `OBJ` shorthand in a derived constraint, completing a `lin` step over column bounds) our choice is stated in the checker's docstring as ours, and conformance with other VIPR tools is not claimed. |
| 27 | CI's Netlib jobs fall back to tracked copies when netlib.org does not answer (`fetch_data.py --offline-fallback`), which needs Netlib's `readme` tracked in `data/netlib/` beside the instances. Is that readme third-party material in the project? | Allowed, recorded | The readme is Netlib's published description of its LP set and the source of the published optima, benchmark data under item 6 of the policy, in the same position as the tracked `.mps` instances; it contains no code. While netlib.org answers, the live readme is parsed and written over the tracked copy, so the published optima still come from Netlib and an edit to the tracked copy cannot move one. Only in an outage are the tracked copies used, with a warning in the log. |
| 28 | Licence check for NVIDIA cuDSS (#489), which the GPU interior point links when built with `SANKHYA_ENABLE_CUDSS=ON`. Read from the `LICENSE.txt` inside the `nvidia-cudss-cu12` 0.8.0.10 wheel on PyPI (published by NVIDIA, `License-Expression: LicenseRef-Nvidia-cuDSS-License`, wheel sha256 `602eb6e394c6595a698ed10b74cf5e251a0fbbd6e84da9a755e02fafb6c60ef1`) and cross-checked against the same agreement on docs.nvidia.com/cuda/cudss/license.html. | **Allowed: link dynamically, optional, default OFF, never vendored or redistributed.** | The licence is the *License Agreement for NVIDIA Math Libraries Software Development Kits* (v. February 10, 2022), with third-party notices for SuiteSparse AMD and COLAMD (BSD-3-Clause), METIS (Apache-2.0), fmt (MIT) and HSL. **Linking from an Apache-2.0 project:** s1.1(i) grants the right to install and use the SDK, and building our code against its header and shared library is use. s2.6 forbids using the SDK so that it becomes subject to an open-source licence (one requiring source disclosure, derivative-work rights or free redistribution of the SDK); Apache-2.0 is not copyleft and places no terms on a separately distributed library our code calls, and no cuDSS header or binary is committed here, so that clause is not engaged. **Redistribution:** s1.1(ii) and s1.2 allow shipping the cuDSS binaries and headers only inside an application with material additional functionality, accessed only by that application, under terms consistent with the agreement; s2.3 forbids any other copying or distribution. We take the conservative reading: the repository, the CI artefacts and any wheel we publish contain no cuDSS file, and a user who builds with the option installs cuDSS from NVIDIA themselves. Bundling it into a release would need this row revisited first. **The header's own notice:** `cudss.h` carries NVIDIA's standard header notice (the same one `cusparse.h` carries, and cuSPARSE is already linked): it forbids disclosing the header to third parties without consent, which not vendoring it satisfies, and asks that software using it carry NVIDIA's disclaimer and U.S. Government End Users notice in its documentation and code comments. `src/gpu/cudss_factor.cpp` and this row point to that notice in the installed header rather than copying it; whether it must be reproduced verbatim is left open here, and applies equally to the existing cuSPARSE use. **Benchmarking and publication:** the agreement has no clause restricting benchmarking, performance measurement or publication of results; the only related restriction is s2.4, no claim that the application is sponsored or endorsed by NVIDIA, which we do not make. **Other terms that bind us:** s2.1 (applications for systems with NVIDIA GPUs: this is the GPU backend and the CPU build does not touch cuDSS); s2.2 (no reverse engineering: we call the documented C API only); s1.4 (cuDSS 0.x is a preview release, not for production: one more reason the backend is opt-in, falls back to the CPU factor on any failure, and is not a default). **Not a solver:** cuDSS solves A x = b for a sparse matrix; it does not formulate or solve an optimization problem, so it sits with cuSOLVER, not with the red line. NVIDIA also ships an optimization solver (cuOpt), and cuDSS's third-party notices name HSL; neither's source was opened, the cuDSS package contains headers and shared libraries only, and our IPM (search direction, step, centrality, termination) stays in `src/ipm/`, written from the papers cited in section 3. |
| 29 | Should the multi-GPU PDHG (#295) sum A^T y across the cards with NCCL? NCCL is an NVIDIA communication library, not a solver, and would be allowed as an optional dependency. | Not used; CUDA peer copies instead | Two reasons, neither about licence. The multi-GPU answer must be bitwise reproducible and the same for every transport, which needs a summation order the code fixes; NCCL chooses ring or tree by message size and topology, so its order is not ours to fix. And on the two-card NVLink box #295 is measured on, an all-gather of the partials by `cudaMemcpyPeerAsync` moves the same bytes over the same link as NCCL's two-rank all-reduce. Revisit for more than one node, where peer copies do not reach. |
| 30 | QPLIB (#492): three things needed a decision. (a) The paper's Appendix B and the files on the site disagree on the quadratic convention: the paper takes Q symmetric (its worked example lists `2 1 -1.0` for a term -x1 x2), while doc.html defines the Q^i as lower-triangular matrices under 0.5 x'Qx, with the footnote that the paper assumed them symmetric. (b) QPLIB's objective values are best known points (`=best=` in `qplib.solu`), not proven optima. (c) The test fixtures include short excerpts of QPLIB's web pages, whose footer reserves the website's rights while stating that QPLIB is CC-BY 4.0. | (a) the site's reading, by measurement; (b) compared against, labelled as best known; (c) allowed, recorded, and yours to overrule | (a) At QPLIB's own published points the triangle reading reproduces the published objective and the symmetric one does not (QPLIB_0018: -6.386014981598351 against -6.38601498159835 published, symmetric -12.87; QPLIB_8845: 10907992.493998824 against 10907992.4939988, symmetric 13461747.4), and the site's GAMS model of QPLIB_0018 writes the file's `2 1 -19.8356` as `-9.9178*x2*x3`. `fetch_qplib.py` repeats the check on every instance through the converted QPS and the verifier's reader, and the runner does not solve an instance where it fails. The GAMS file was read as instance data; GAMS is a modelling system and no solver source was opened. (b) Every CSV row carries `reference_source` saying so, and an objective below the best known value by more than 1e-6 is reported as a disagreement, not a win. (c) The excerpts hold the instance table rows and identifier tables for the parser tests - data the site publishes under CC-BY 4.0 - plus the two lines of index.html stating the instance counts and the licence; no page is copied whole. If that reading of the footer is too generous, the fixtures can be replaced by synthetic pages in the same markup at the cost of testing against the site's own. |
| 31 | NLP stage 1 (the `.nl` reader, the Hessian of the Lagrangian, the compiled tapes): what was read, given that the .nl format belongs to AMPL and the reference reader ships inside the AMPL/solver interface library, and that several NLP solvers read .nl files? | Written from two documents only; no solver source read | The reader is written from Gay's "Writing .nl Files" (SAND2005-7907P), a technical report describing the format for people writing it without AMPL, and the variable-ordering tables of Gay's "Hooking Your Solver to AMPL", a manual; both were read as PDFs from ampl.github.io. The AMPL/solver interface library (netlib `ampl/solvers`), which both documents point to as the reference implementation, was NOT read: it is an interface library rather than a solver, but it is distributed together with solver drivers, and the specification was sufficient. Three operator codes AMPL is known to emit (the specialised powers) are not in either document and are refused rather than guessed. No solver's .nl reader or NLP code was opened: not Ipopt, KNITRO, CONOPT, SNOPT, Bonmin, Couenne, BARON, filterSQP, SCIP, or any solver on the red line of section 1. The derivatives are written from Griewank & Walther and Walther (2008); no AD tool's source (ADOL-C, CppAD, CasADi) was read either, though none is a solver. |

---

## 6. Reproducing this

```bash
scripts/configure.sh build Release
cmake --build build -j
ctest --test-dir build --output-on-failure
```

On Linux, `ldd build/sankhya`. On Windows, `objdump -p build/sankhya.exe | grep "DLL Name"`.

To reproduce the Phase 2 end-to-end result:

```bash
build/sankhya info demo/crude_blend.mps
build/sankhya solve demo/crude_blend.mps --write-sol blend.sol --stats blend.json
build/sankhya solve demo/crude_blend.lp
```

The MPS and LP files describe the same model; the two solutions must be identical.
